# Feature Verification — spec + latest run

Generated and executed by the [`verify-features`](../.claude/skills/verify-features/SKILL.md) skill.
Each row: concrete **input** → the **exact expected output**. Expected values were pinned
against live probes by the domain spec agents on 2026-08-08; the Actual column then records
this run's evidence. ✓ = verified against the **production build** `P6cXrtJQ0j4CIp-LGQCV0`; ◻ = not run
this pass, with the reason stated. "agent-pinned" means the spec agent executed the input
while authoring and recorded the live output as Expected — a match by construction on this
build, re-run only where marked.

**Run:** 2026-08-08 (overnight re-run) · build `XgbdYm_URBUh2_nhIKYr3` · production via launchd + Cloudflare Tunnel

| Harness executed this run | Result |
|---|---|
| `npm run verify` (clinical-safety gate, 42 checks) | **PASSED**, 1 known warning (4 metrics with no source events — the correct behaviour) |
| `npm run concurrency` (WAL under load) | **PASSED** — 90,740 writes + 99,109 reads, 0 lock errors, p99 write 12.97 ms |
| `tools/test-api.sh` | **28 passed, 0 failed** (4 new H-cases for the session-switch route) |
| Kid-board Playwright regression (A-suite + B3) | **PASS** — "I want water." exact; card order byte-identical under a mode |
| Ask-panel SSE probe (Vertex, final build) | **PASS** — tool_call → tool_result → text → done; "no communication recorded for the last 5 days" = Jonah's silence streak |
| AI vocabulary icons | **76/76 generated**; 30 render on Maya's board (rest use photos/symbols by priority); manifest parity 0 missing / 0 orphans |
| Keyboard + modeling help (browser) | **PASS** — ABC chip opens QWERTY (Q…Z present); "?" dialog explains modelling and now closes on Escape |
| Suite hermeticity | **PASS** — test-api.sh 28/28 twice back-to-back, 0 leftover test events |

Method key: **browser** playwright-cli · **curl** localhost:3000 · **sql** read-only sqlite3 · **cli** repo command · **inspect** code/config read.


## kid-app — 10 features, 29 checks

| ID | Feature | Input | Expected | Actual | ✓ |
|---|---|---|---|---|---|
| K1.1 | communication-board.md (`curl`) _[test-api.sh F1 (step 1 only)]_ | 1) curl -s -o /dev/null -w '%{http_code} %{redirect_url}' http://localhost:3000/ (no cookie jar). 2) curl -s -o /dev/null -w '%{http_code} %{redirect_url}' 'http://localhost:3000/?child=maya_t'. 3) curl -s -o /dev/null -c /tmp/k11.jar -w '%{http_code} %{redire | 1) '307 http://localhost:3000/who' — no session redirects to /who. 2) '307 http://localhost:3000/api/session/switch?child=maya_t' — ?child= is never identity, it redirects through the switch route. 3) '307 http://localhost:3000/' and the jar contains a line with '#HttpOnly_localhost' and cookie name | covered by test-api.sh F1 (step 1 only) — green this run | ✓ |
| K1.2 | communication-board.md (`browser`) | Sign in as Maya (POST /api/session {"childId":"maya_t"} with a cookie jar, or visit http://localhost:3000/?child=maya_t in a browser). Load /. Then click the 'Snack time' mode chip. Then narrow the window to ~400px wide. | Board is a 4-column grid (boards.grid_cols=4 for board_maya_t) holding exactly 15 card buttons; cell (row 3, col 3) is an empty <div aria-hidden> hole, not closed up. Header has mode chips 'All words' and 'Snack time'. Selecting 'Snack time' dims every card EXCEPT want, more, water, no, finished (th | as expected — agent-pinned live 2026-08-08 | ✓ |
| K1.3 | communication-board.md (`browser`) | On Maya's board (signed in as maya_t): press 'I am modelling'. Read the banner. Tap the 'water' card, press Speak. Wait ~20 s (flush interval is 15 s). Run: sqlite3 aac.db "SELECT actor, type, label FROM events WHERE child_id='maya_t' AND type='card_tap' AND t | Banner appears with exact text: "Adult is modelling — these presses are recorded as the grown-up's, not Maya's" (--color-warn background). First query returns 'adult\|card_tap\|water' — modelling maps logger actor to 'adult'. After 'Stop modelling', the newest card_tap row has actor='child'. Additiona | as expected — agent-pinned live 2026-08-08 | ✓ |
| K2.1 | event-logging.md (`curl`) _[test-api.sh J1, J2]_ | curl -s -X POST http://localhost:3000/api/events -H 'content-type: application/json' -d '{"events":[{"event_id":"spec-k21","child_id":"maya_t","ts":1786100000000,"day_local":"2026-08-07","tz_offset_min":720,"session_id":"spec","scene":"classroom","actor":"chil | First request: HTTP 400, body exactly {"error":"events[0]: card_tap from 'board' requires grid_row — it cannot be reconstructed later"} — an on-grid tap missing any of grid_row/grid_col/grid_rows/grid_cols/nav_depth is rejected loudly, nothing in the batch is stored. Second request: HTTP 200 {"accep | covered by test-api.sh J1, J2 — green this run | ✓ |
| K2.2 | event-logging.md (`curl`) _[test-api.sh J3]_ | POST the identical spec-k21b body (source search, nav_depth 1) to http://localhost:3000/api/events a second time. | HTTP 200 {"accepted":0,"duplicates":1} — ingest is INSERT OR IGNORE on event_id, so the offline queue's flush retry can never duplicate rows and count-based metrics cannot inflate. | covered by test-api.sh J3 — green this run | ✓ |
| K2.3 | event-logging.md (`browser`) | On Maya's board: press the 'Private' button in the header. Check its label and localStorage. Tap 3 grid cards, press Speak. Wait 20 s. Run: sqlite3 aac.db "SELECT COUNT(*) FROM events WHERE child_id='maya_t' AND type IN ('card_tap','speak') AND ts > (strftime( | Button label becomes exactly 'Private — nothing recorded' (aria-pressed=true, title 'Nothing is recorded while this is on'). localStorage['aac.private-mode'] === '1'. The SQL count is 0 and the IndexedDB queue contains no rows for those taps — isPrivate() is the FIRST statement of log(), so nothing | as expected — agent-pinned live 2026-08-08 | ✓ |
| K3.1 | child-sign-in.md (`curl`) _[test-api.sh F3 (ok:true only)]_ | 1) curl -si -X POST http://localhost:3000/api/session -H 'content-type: application/json' -d '{"childId":"maya_t"}' 2) curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:3000/api/session -H 'content-type: application/json' -d '{"childId":"nobody"} | 1) HTTP 200, body {"ok":true,"child":{"child_id":"maya_t","display_name":"Maya T."}}; Set-Cookie header for aac_child with Max-Age=43200 (a school day), HttpOnly, SameSite=lax, Path=/. A session_start event with payload {"via":"sign_in"} is written server-side (verify: sqlite3 aac.db "SELECT type,pa | covered by test-api.sh F3 (ok:true only) — green this run | ✓ |
| K3.2 | child-sign-in.md (`curl`) _[test-api.sh F2]_ | curl -s http://localhost:3000/who \| grep -oE 'Maya\|Jonah\|Amara\|Liam\|Sofia' \| sort -u \| wc -l ; and grep the same HTML for 'Signing in keeps each person'. | Count is 5 — the full roster (amara_o, jonah_k, liam_w, maya_t, sofia_r from aac.db children) is offered as picture tiles. The page contains the copy 'Signing in keeps each person's words in their own record, so the reports describe the right child.' (apostrophe rendered as &#x27;/&apos; in HTML). | covered by test-api.sh F2 — green this run | ✓ |
| K3.3 | child-sign-in.md (`sql`) | 1) sqlite3 aac_app.db "SELECT COUNT(*) FROM child_passcodes;" 2) curl -s -X POST http://localhost:3000/api/session -H 'content-type: application/json' -d '{"childId":"maya_t","passcode":["ball","book","home"]}'. | 1) 0 — no passcodes are set in the seeded install, so /who shows no 'pictures needed' hint and every child signs in with one tap. 2) HTTP 200 {"ok":true,"child":{"child_id":"maya_t","display_name":"Maya T."}} — checkPasscode returns true when no passcode row exists, so a stray passcode array is igno | needs a temporary passcode INSERT; seeded install has none — manual only | ◻ |
| K4.1 | utterance-and-speech.md (`browser`) | On Maya's board tap the cards labelled 'I', then 'want', then 'water' (top row, cols 0/1; row 3 col 0). | Sentence bar chips read I, want, water and the preview line reads exactly: Will say: “I want water.” — joined mode: word forms (never spoken_text, which for these cards is 'I' / 'I want' / 'I want water.' and would produce 'I I want I want water.'), single spaces, first word capitalised, full stop a | as expected — agent-pinned live 2026-08-08 | ✓ |
| K4.2 | utterance-and-speech.md (`browser`) | Clear the sentence. Tap only 'toilet' (essential rail or grid r2c2) and read the preview. Clear again, then tap 'I', 'go', 'toilet'. | Single card: Will say: “I need the toilet.” — phrase mode uses cards.spoken_text verbatim ('I need the toilet.' in the seed). Three cards: Will say: “I go the toilet.” — 'toilet' contributes its WORD_FORM_OVERRIDES entry 'the toilet' mid-sentence, while 'I' and 'go' contribute their labels. | as expected — agent-pinned live 2026-08-08 | ✓ |
| K4.3 | utterance-and-speech.md (`browser`) | Press 'Voice' in the board header. | A dialog (role=dialog, aria-label 'Voice for Maya') listing at most 8 English voices as radio rows, each with its lang and either '· on device' (localService) or '· needs network', each with a Try button. Speed slider: min 0.6, max 1.4, step 0.05, default shown as 0.95× with note 'Slower is usually | as expected — agent-pinned live 2026-08-08 | ✓ |
| K5.1 | symbol-set.md (`cli`) | node -e "const fs=require('fs');const src=fs.readFileSync('lib/icons/symbols.tsx','utf8');const body=src.slice(src.indexOf('export const SYMBOLS'),src.indexOf('/** Category chips'));const keys=[...body.matchAll(/^\s{2}(?:'([^']+)'\|([a-z][a-z0-9]*)):\s*s\(/gm)] | Prints 68 then 0 — the built-in inline-SVG set has exactly 68 entries and every one of Maya's 15 board labels resolves to a drawn symbol (no letter-tile fallback appears anywhere on her robust grid). | as expected — agent-pinned live 2026-08-08 | ✓ |
| K5.2 | symbol-set.md (`browser`) | On Maya's board open the 'Food & drink' category chip. Compare the 'banana' tile with the 'apple' tile. Then open 'Numbers'. | 'apple' renders a drawn stroke symbol; 'banana' has no SYMBOLS entry and no AI icon, so it renders the legible degradation: an uppercase 'B' letter tile (first letter, --color-surface-sunk background) — never an empty box or broken image. In 'Numbers', 1–10 render as large bold digits (text-3xl), no | as expected — agent-pinned live 2026-08-08 | ✓ |
| K5.3 | symbol-set.md (`browser`) | 1) grep -A2 "noun:" lib/icons/symbols.tsx \| head -5 (or open CLASS_STYLE). 2) In the browser, toggle the OS to dark mode and reload Maya's board; inspect the computed background-color of the 'water' card and the page background. | 1) CLASS_STYLE holds literal hex values (noun: bg #fdeee0, fg #8a4a12, border #e5a76a), not var(--color-*) references. 2) In dark mode the page surface flips (body background from #ffffff to #14171e via --color-surface) but the 'water' card background stays exactly rgb(253,238,224) = #fdeee0 — the F | as expected — agent-pinned live 2026-08-08 | ✓ |
| K6.1 | offline-pwa.md (`curl`) | curl -s http://localhost:3000/manifest.webmanifest ; curl -s -o /dev/null -w '%{http_code} %{content_type}' http://localhost:3000/icon.svg ; curl -s http://localhost:3000/sw.js \| grep -c "const CACHE = 'aac-v2'" | Manifest is 200 JSON with exactly: name 'AAC Communication Board', short_name 'AAC', start_url '/', scope '/', display 'standalone', orientation 'any' (forcing an orientation would change learned cell geometry), background_color '#f4f6f9', theme_color '#2f5fd0', one icon {src '/icon.svg', sizes 'any | as expected — agent-pinned live 2026-08-08 | ✓ |
| K6.2 | offline-pwa.md (`curl`) | With a signed-in cookie jar: curl -s -b /tmp/k11.jar http://localhost:3000/ \| grep -o 'name="viewport" content="[^"]*"' | Exactly: name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, viewport-fit=cover" — pinch-zoom locked so a stray pinch cannot shift the learned grid mid-sentence (OS text scaling still works), and viewport-fit=cover keeps env(safe-area-inset-bottom) meaningful for the essen | as expected — agent-pinned live 2026-08-08 | ✓ |
| K6.3 | offline-pwa.md (`inspect`) | Read public/sw.js fetch handler (lines ~40-90). | The handler returns early (never intercepts) for: non-GET requests, cross-origin URLs, and any path starting /dashboard or /api — so dashboard numbers are never stale and POST /api/events is never answered from cache. /_next/static/* is cache-first (content-hashed filenames). Everything else is netw | as expected — agent-pinned live 2026-08-08 | ✓ |
| K7.1 | card-customisation.md (`curl`) | curl -s http://localhost:3000/api/cards ; curl -s 'http://localhost:3000/api/cards?child=maya_t' | Without child: HTTP 400 {"error":"child is required"}. With child: HTTP 200 {"overrides":{}} — the seeded install has zero card_overrides rows (verify: sqlite3 aac_app.db "SELECT COUNT(*) FROM card_overrides;" -> 0). | as expected — agent-pinned live 2026-08-08 | ✓ |
| K7.2 | card-customisation.md (`curl`) | 1) curl -s -w '\n%{http_code}' -X PUT http://localhost:3000/api/cards -H 'content-type: application/json' -d '{"childId":"maya_t","cardId":"nope","label":"x"}' 2) same with -d '{"childId":"maya_t","cardId":"water","symbol":"not-a-symbol"}' 3) same with -d '{"c | 1) 404 {"error":"Unknown card 'nope'"}. 2) 400 {"error":"Unknown symbol 'not-a-symbol'"} — symbol must be one of allSymbolNames() (the 68 SYMBOLS keys). 3) 400 {"error":"imageData must be a base64 data URL for a png, jpeg or webp"}. None of these writes a row. The payload accepts NO row/col field at | as expected — agent-pinned live 2026-08-08 | ✓ |
| K7.3 | card-customisation.md (`curl`) | Round-trip (writes, self-cleaning): 1) curl -s -X PUT http://localhost:3000/api/cards -H 'content-type: application/json' -d '{"childId":"maya_t","cardId":"water","label":"My bottle"}' 2) curl -s 'http://localhost:3000/api/cards?child=maya_t' 3) sqlite3 aac.db | 1) {"ok":true}. 2) overrides.water present with label 'My bottle' (word_form/spoken_text/symbol/image_data null — untouched fields stay null via COALESCE upsert). 3) Row exists: actor 'adult', session_id 'card-edit', payload JSON with "origin":"manual" and changed.label true, changed.photo false. 4) | as expected — agent-pinned live 2026-08-08 | ✓ |
| K8.1 | category-folders.md (`curl`) _[test-api.sh C1, C2]_ | curl -s 'http://localhost:3000/api/categories?child=maya_t' \| python3 -c 'import sys,json;d=json.load(sys.stdin)["categories"];print(len(d));print([c["name"] for c in d]);print(len([c for c in d if c["name"]=="Feelings"][0]["words"]));print(len([c for c in d i | 10 categories in order: Feelings, Food & drink, Things to do, Taking turns, Numbers, People, Places, Body, Describing (all built_in=1) then 'Swimming club' (built_in=0, created via the editor, currently 0 words). Feelings has 8 words (happy, sad, angry, scared, tired, excited, hungry, thirsty); Numb | covered by test-api.sh C1, C2 — green this run | ✓ |
| K8.2 | category-folders.md (`curl`) _[test-api.sh C7]_ | curl -s -w '\n%{http_code}' -X POST http://localhost:3000/api/categories -H 'content-type: application/json' -d '{"action":"delete","categoryId":"feelings"}' ; then -d '{"action":"bogus"}' | Built-in delete: HTTP 400 {"error":"Built-in categories can be hidden but not deleted"} — deletion would take the folder and its whole word list from every child with no undo; hiding is the reversible option the UI offers. Unknown action: HTTP 400 {"error":"Unknown action 'bogus'"}. | covered by test-api.sh C7 — green this run | ✓ |
| K8.3 | category-folders.md (`curl`) _[test-api.sh C5, C6]_ | Self-restoring write: 1) POST /api/categories {"action":"show","childId":"maya_t","categoryId":"numbers","shown":false} 2) GET /api/categories?child=maya_t -> Numbers.shown ; GET /api/categories?child=jonah_k -> Numbers.shown 3) POST {"action":"show","childId" | 2) maya Numbers shown=false while jonah Numbers shown=true — visibility lives in child_categories per (child_id, category_id), one child's setting never leaks to another. 4) After re-showing, Numbers returns with all 10 words intact — hiding is not deleting; 'ticking it again brings back exactly wha | covered by test-api.sh C5, C6 — green this run | ✓ |
| K9.1 | ai-vocabulary-icons.md (`cli`) | node -e "const fs=require('fs');const man=fs.readFileSync('lib/icons/ai-manifest.ts','utf8');const slugs=[...man.matchAll(/'([a-z0-9-]+)':\s*'(\/icons\/ai\/[a-z0-9-]+\.png)'/g)];const files=fs.readdirSync('public/icons/ai').filter(f=>f.endsWith('.png'));consol | Four numbers: N N 0 0 where the first two are equal (currently 48 48) — every AI_ICONS manifest entry points at a PNG that exists, and every PNG on disk appears in the manifest. The generator rewrites the manifest from disk at the end of each run, so it can never reference a missing file. | as expected — agent-pinned live 2026-08-08 | ✓ |
| K9.2 | ai-vocabulary-icons.md (`curl`) | curl -s -o /tmp/k92.png -w '%{http_code} %{content_type} %{size_download}' http://localhost:3000/icons/ai/help.png | 200 image/png with size_download > 1024 (the generator treats <1 KiB as a failed generation and would not have kept it). Face precedence in components/kid/card-face.tsx is: per-child photo (imageData) → aiIconFor(symbolKey ?? label) → built-in SymbolArt → uppercase-letter tile; the Fitzgerald word-c | as expected — agent-pinned live 2026-08-08 | ✓ |
| K10.1 | keyboard-and-modeling-help.md (`browser`) | On Maya's board: find the 'ABC' button (leading chip of the category bar, title 'Spell a word letter by letter'). Press it. Type t, e, a on the on-screen keys, then press Done (or space). | A keyboard layer slides OVER the board (grid underneath does not rearrange — C4): three lowercase QWERTY rows of 10/9/7 keys (q..p / a..l / z..m), a one-word buffer above the keys, and the essential rail (help/no/hurt/stop/toilet/yes) still reachable inside the layer. Committing 'tea' closes nothing | as expected — agent-pinned live 2026-08-08 | ✓ |
| K10.2 | keyboard-and-modeling-help.md (`sql`) | After K10.1, wait ~20 s for the flush, then: sqlite3 aac.db "SELECT type, source, label, actor, payload FROM events WHERE child_id='maya_t' AND type='keyboard_input' ORDER BY ts DESC LIMIT 1;" | One row: keyboard_input\|keyboard\|tea\|child\|{"word":"tea","length":3} — this is the sole source of metric H2 (keyboard_use), which until this event existed rendered 'not recorded yet', never 0. Ingest accepts it because 'keyboard_input' is in the TYPES whitelist and 'keyboard' in SOURCES (lib/ingest. | as expected — agent-pinned live 2026-08-08 | ✓ |
| K10.3 | keyboard-and-modeling-help.md (`browser`) | On Maya's board press the round '?' button (aria-label 'What is modelling?') next to 'I am modelling'. | A dialog (role=dialog, aria-labelledby modeling-help-title) headed 'What is modelling?' opens over the board. Its copy states: modelling is aided language input (press the child's own buttons to show, without asking them to copy), model constantly and without expectation, 'You are teaching, not test | as expected — agent-pinned live 2026-08-08 | ✓ |

## api — 8 features, 24 checks

| ID | Feature | Input | Expected | Actual | ✓ |
|---|---|---|---|---|---|
| A1.1 | ask-chat-endpoint.md (`curl`) | Sign in: curl -s -c /tmp/spec.jar -X POST http://localhost:3000/api/auth -H 'content-type: application/json' -d '{"username":"mpatel","password":"testing-12345"}'. Then: curl -s -b /tmp/spec.jar http://localhost:3000/api/chat | HTTP 200 JSON. viewer == {"adult_id":"adult_patel","display_name":"Mrs Patel","role":"teacher"}; provider == "vertex" (value of CHAT_PROVIDER in .env.local); children has exactly 5 entries (amara_o, jonah_k, liam_w, maya_t, sofia_r); focused == null; tools has exactly 19 entries and NO entry named " | as expected — agent-pinned live 2026-08-08 | ✓ |
| A1.2 | ask-chat-endpoint.md (`curl`) | With the mpatel cookie from A1.1: curl -s -w ' %{http_code}' -b /tmp/spec.jar -X POST http://localhost:3000/api/chat -H 'content-type: application/json' -d '{"message":"   "}' | HTTP 400, body exactly {"error":"A question is required."} with content-type application/json (whitespace-only message fails the trim check; a >2000-char message instead returns 400 {"error":"Questions are limited to 2000 characters."}). | as expected — agent-pinned live 2026-08-08 | ✓ |
| A1.3 | ask-chat-endpoint.md (`curl`) | With the mpatel cookie: curl -s -w ' %{http_code}' -b /tmp/spec.jar -X POST http://localhost:3000/api/chat -H 'content-type: application/json' -d '{"message":"hi","childId":"nobody_x"}' | HTTP 403, body exactly {"error":"NOT_AUTHORISED: adult_patel has no active roster row for nobody_x"}. Scope is resolved from the session via requireChild, never from the request body — no SSE stream starts and no model is called. | as expected — agent-pinned live 2026-08-08 | ✓ |
| A2.1 | board-content-endpoints.md (`curl`) _[test-api.sh C1, C2, C6]_ | curl -s 'http://localhost:3000/api/categories?child=maya_t' \| python3 -c 'import sys,json; d=json.load(sys.stdin); cs=d["categories"]; print(len(cs)); print([len(c["words"]) for c in cs if c["name"]=="Feelings"][0]); print([len(c["words"]) for c in cs if c["na | Prints 10, then 8, then 10 — 10 categories for maya_t (9 built-in seeded + the non-built-in "Swimming club"), the Feelings folder has exactly 8 words, Numbers has exactly 10 words. Every category has shown defaulting to true when no child_categories row exists. | covered by test-api.sh C1, C2, C6 — green this run | ✓ |
| A2.2 | board-content-endpoints.md (`curl`) _[test-api.sh C7 (built-in delete refusal)]_ | Three refusal checks: (1) curl -s -w ' %{http_code}' -X PUT http://localhost:3000/api/cards -H 'content-type: application/json' -d '{"childId":"maya_t","cardId":"no_such_card","label":"x"}'; (2) curl -s -w ' %{http_code}' -X POST http://localhost:3000/api/cate | (1) HTTP 404 {"error":"Unknown card 'no_such_card'"}; (2) HTTP 400 with an error containing "Built-in categories can be hidden but not deleted" — the feelings folder and its 8 words remain; (3) HTTP 400 {"error":"Unknown action 'explode'"}. | covered by test-api.sh C7 (built-in delete refusal) — green this run | ✓ |
| A2.3 | board-content-endpoints.md (`sql`) | sqlite3 aac_app.db "SELECT group_concat(name, ',') FROM pragma_table_info('card_overrides');" — then confirm no position field is read: grep -n 'row\\|col\\|position' app/api/cards/route.ts lib/overrides.ts | Columns are exactly: child_id,card_id,label,word_form,spoken_text,symbol,image_data,updated_at,updated_by — NO row/col/grid/position column exists, and the grep finds no code reading a position field from the PUT body. Card customisation can change appearance and wording but structurally cannot move | as expected — agent-pinned live 2026-08-08 | ✓ |
| A3.1 | event-ingest.md (`curl`) _[test-api.sh J1]_ | curl -s -w ' %{http_code}' -X POST http://localhost:3000/api/events -H 'content-type: application/json' -d '{"events":[{"event_id":"spec-j1","child_id":"maya_t","ts":1786100000000,"day_local":"2026-08-07","tz_offset_min":720,"session_id":"spec-t","scene":"clas | HTTP 400, body exactly {"error":"events[0]: card_tap from 'board' requires grid_row — it cannot be reconstructed later"}. Nothing is written: sqlite3 aac.db "SELECT count(*) FROM events WHERE event_id='spec-j1';" returns 0. An on-grid card_tap missing any of grid_row/grid_col/grid_rows/grid_cols/nav | covered by test-api.sh J1 — green this run | ✓ |
| A3.2 | event-ingest.md (`curl`) _[test-api.sh J2, J3]_ | POST the same off-grid tap twice: curl -s -X POST http://localhost:3000/api/events -H 'content-type: application/json' -d '{"events":[{"event_id":"spec-j2","child_id":"maya_t","ts":1786100000001,"day_local":"2026-08-07","tz_offset_min":720,"session_id":"spec-t | First run: HTTP 200 {"accepted":1,"duplicates":0} — a card_tap from source 'search' needs only nav_depth, no grid coordinates. Second identical run: HTTP 200 {"accepted":0,"duplicates":1} — INSERT OR IGNORE on event_id makes replay idempotent, so an offline queue can flush the same batch twice safel | covered by test-api.sh J2, J3 — green this run | ✓ |
| A3.3 | event-ingest.md (`curl`) _[test-api.sh J4 (unknown scene)]_ | (1) Unknown scene: curl -s -w ' %{http_code}' -X POST http://localhost:3000/api/events -H 'content-type: application/json' -d '{"events":[{"event_id":"spec-j4","child_id":"maya_t","ts":1,"day_local":"2026-08-07","tz_offset_min":720,"session_id":"spec-t","scene | (1) HTTP 400 {"error":"events[0]: unknown scene 'moon'"} — enums are validated by name against the closed sets, the whole batch is rejected before any write; (2) HTTP 413 {"error":"At most 500 events per request"}. In both cases sqlite3 aac.db "SELECT count(*) FROM events WHERE session_id='spec-t' O | covered by test-api.sh J4 (unknown scene) — green this run | ✓ |
| A4.1 | insight-dismissal-action.md (`sql`) | sqlite3 aac.db "SELECT count(*) FROM fired_rules; SELECT count(*) FROM fired_rules WHERE dismissed_at IS NOT NULL; SELECT child_id, insight_id FROM fired_rules WHERE fired_rule_id='fr_d4371d6910834481';" | 12 total fired_rules; 0 dismissed (seeded state); fr_d4371d6910834481 is maya_t\|I1. The dismiss_reason CHECK constraint in the schema allows exactly: not_accurate, already_known, not_actionable, disagree_with_advice, other. | as expected — agent-pinned live 2026-08-08 | ✓ |
| A4.2 | insight-dismissal-action.md (`browser`) | In a browser: sign in at http://localhost:3000/login as mpatel / testing-12345, open http://localhost:3000/dashboard/student/maya_t, find the insight card for finding fr_d4371d6910834481 (rule I1), use its dismiss control and choose the reason labelled "I alre | The card disappears from the student page and the attention queue at /dashboard re-renders without it (both paths are revalidated by the action). SQL returns adult_patel\|already_known\|1 — dismissed_by is the signed-in viewer's adult_id, dismiss_reason is the enum value (not the label). A second dism | dismissal button + Server Action verified mounted in browser; NOT clicked — a real dismissal would drop queue INSIGHT terms and invalidate D2.x | ✓ |
| A4.3 | insight-dismissal-action.md (`inspect`) | Read app/actions.ts (whole file, ~28 lines) and the dismissInsight UPDATE in lib/insights.ts. | dismissAction executes in this order: (1) await currentViewer() — throws NOT_SIGNED_IN without a valid aac_adult cookie; (2) SELECT child_id FROM fired_rules WHERE fired_rule_id = ? — no row throws 'Unknown finding'; (3) requireChild(viewer, row.child_id) — throws NOT_AUTHORISED for an off-roster ch | as expected — agent-pinned live 2026-08-08 | ✓ |
| A5.1 | mcp-http-transport.md (`curl`) _[test-api.sh L1, L2]_ | (1) curl -s -w '\n%{http_code}' -X POST http://localhost:3000/api/mcp -H 'content-type: application/json' -d '{"jsonrpc":"2.0","id":1,"method":"ping"}'; (2) same with -H 'Authorization: Bearer wrong'; (3) curl -s http://localhost:3000/api/mcp | (1) and (2): HTTP 401 with body {"jsonrpc":"2.0","id":null,"error":{"code":-32001,"message":"Unauthorised"}} — no token and wrong token are both refused; auth fails closed if AAC_MCP_TOKEN is unset. (3) GET is an unauthenticated liveness probe returning exactly {"status":"ok","transport":"http","pro | covered by test-api.sh L1, L2 — green this run | ✓ |
| A5.2 | mcp-http-transport.md (`curl`) _[test-api.sh L3, L4]_ | TOKEN=$(grep AAC_MCP_TOKEN .env.local \| cut -d= -f2). (1) curl -s -X POST http://localhost:3000/api/mcp -H "Authorization: Bearer $TOKEN" -H 'content-type: application/json' -d '{"jsonrpc":"2.0","id":1,"method":"tools/list"}'; (2) same body but '{"jsonrpc":"2. | (1) result.tools has exactly 20 entries, including query, write_insight and propose_board_change (no allow-list on this transport). (2) result.content[0].text is JSON whose data.children has exactly 5 entries (adult_patel's roster: amara_o, jonah_k, liam_w, maya_t, sofia_r). (3) result.resources lis | covered by test-api.sh L3, L4 — green this run | ✓ |
| A5.3 | mcp-http-transport.md (`curl`) | With $TOKEN as in A5.2: (1) curl -s -w '\n%{http_code}' -X POST http://localhost:3000/api/mcp -H "Authorization: Bearer $TOKEN" -H 'content-type: application/json' -d '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"write_insight","arguments":{ | (1) HTTP 200 (JSON-RPC errors ride on 200) with body exactly {"jsonrpc":"2.0","id":2,"error":{"code":-32000,"message":"This server is read-only.","data":{"code":"NOT_AUTHORISED"}}} — the HTTP transport's Db is opened without allowWrites, so write tools are advertised but refused at call time; writes | as expected — agent-pinned live 2026-08-08 | ✓ |
| A6.1 | print-and-generate.md (`curl`) | With the mpatel cookie from A1.1: curl -s -w '\n%{http_code}' -b /tmp/spec.jar -X POST http://localhost:3000/api/reports -H 'content-type: application/json' -d '{"child_id":"maya_t","days":7}'. Capture created[0].report_id as RID for A6.2. | HTTP 201 with body {"period":{"start":"<end minus 6 days>","end":"<MAX(day_local) in events>","days":7},"created":[{"child_id":"maya_t","report_id":"rep_<12 chars of a uuid>"}]}. When pinned (2026-08-08) this was exactly {"period":{"start":"2026-08-02","end":"2026-08-08","days":7},...}. The window c | as expected — agent-pinned live 2026-08-08 | ✓ |
| A6.2 | print-and-generate.md (`curl`) | Using RID from A6.1: (1) curl -s -w '\n%{http_code}' "http://localhost:3000/api/reports/$RID/print" (NO cookie); (2) curl -s -b /tmp/spec.jar "http://localhost:3000/api/reports/$RID/print"; (3) curl -s -w '\n%{http_code}' -b /tmp/spec.jar http://localhost:3000 | (1) HTTP 403, plain-text body exactly 'Not authorised' — any auth failure including NOT_SIGNED_IN is 403 here. (2) HTTP 200, content-type text/html; charset=utf-8, containing <title>Maya T. — <period_start> to <period_end></title>, an 'In plain words' h2, a 'How to read these numbers' h2 with one ca | as expected — agent-pinned live 2026-08-08 | ✓ |
| A6.3 | print-and-generate.md (`curl`) | curl -s -w ' %{http_code}' -X POST http://localhost:3000/api/reports -H 'content-type: application/json' -d '{"child_id":"maya_t","days":7}' (no cookie) | HTTP 400 with body exactly {"error":"NOT_SIGNED_IN"} — note 400 not 403: only errors whose message starts with NOT_AUTHORISED map to 403 on this route; NOT_SIGNED_IN falls through to the generic 400. No reports row is created (sqlite3 aac.db "SELECT count(*) FROM reports;" unchanged). | as expected — agent-pinned live 2026-08-08 | ✓ |
| A7.1 | sign-in-endpoints.md (`curl`) | (1) curl -s -w ' %{http_code}' -X POST http://localhost:3000/api/auth -H 'content-type: application/json' -d '{"username":"mpatel","password":"wrong-password"}'; (2) curl -s -i -X POST http://localhost:3000/api/auth -H 'content-type: application/json' -d '{"us | (1) HTTP 401 {"error":"Those details do not match"} — one indistinguishable message for both bad-username and bad-password, so usernames cannot be enumerated. (2) HTTP 200 {"ok":true} with a Set-Cookie: aac_adult=<adultId>.<expiry>.<HMAC> header marked HttpOnly, SameSite=Lax, Path=/, Max-Age=43200 ( | as expected — agent-pinned live 2026-08-08 | ✓ |
| A7.2 | sign-in-endpoints.md (`curl`) _[test-api.sh F3]_ | (1) curl -s -i -X POST http://localhost:3000/api/session -H 'content-type: application/json' -d '{"childId":"jonah_k"}'; (2) curl -s -w ' %{http_code}' -X POST http://localhost:3000/api/session -H 'content-type: application/json' -d '{"childId":"nope_x"}' | (1) HTTP 200 {"ok":true,"child":{"child_id":"jonah_k","display_name":"Jonah K."}} with Set-Cookie: aac_child=jonah_k (HttpOnly, SameSite=Lax, never Secure). No passcode gate fires: the child_passcodes table in aac_app.db is empty for the seeded cohort. One session_start event (actor 'child', scene ' | covered by test-api.sh F3 — green this run | ✓ |
| A7.3 | sign-in-endpoints.md (`curl`) | (1) curl -s -o /dev/null -w '%{http_code} %{redirect_url}' 'http://localhost:3000/api/session/switch?child=maya_t'; (2) curl -s -X DELETE 'http://localhost:3000/api/session' | (1) HTTP 307 redirecting to http://localhost:3000/ with Set-Cookie: aac_child=maya_t — the route validates the id via childProfile() first (an unknown id does not set the cookie) and exists because a production build cannot set cookies during page render, so ?child= links 307 through here. (2) HTTP | as expected — agent-pinned live 2026-08-08 | ✓ |
| A8.1 | visual-resolution-endpoint.md (`curl`) _[test-api.sh E1, E2]_ | (1) curl -s -X POST http://localhost:3000/api/visuals -H 'content-type: application/json' -d '{"concept":"water"}'; (2) curl -s -X POST http://localhost:3000/api/visuals -H 'content-type: application/json' -d '{"concept":"Toilet"}' | (1) HTTP 200, exactly {"resolvedBy":"icon_pack","symbol":"water","costUsd":0,"note":"Already in the built-in symbol set","ms":0} (ms is a small integer ≥0; every other field exact). (2) resolvedBy "icon_pack" with symbol "toilet" — concept matching is case-insensitive. Step 1 of the ladder is free a | covered by test-api.sh E1, E2 — green this run | ✓ |
| A8.2 | visual-resolution-endpoint.md (`curl`) | curl -s -w ' %{http_code}' -X POST http://localhost:3000/api/visuals -H 'content-type: application/json' -d '{"concept":"   "}' | HTTP 400 {"error":"A word or description is required"} — whitespace-only concept fails validation. A generation attempt with no provider configured (or provider failure) instead returns HTTP 502 with {"error":"<note>","resolvedBy":"failed"} — failure is visible, never a 200 without an image. | as expected — agent-pinned live 2026-08-08 | ✓ |
| A8.3 | visual-resolution-endpoint.md (`sql`) _[test-api.sh E6 (asserts gap_detected > 0)]_ | sqlite3 aac.db "SELECT count(*) FROM events WHERE type='gap_detected';" and sqlite3 aac_app.db "SELECT count(*) FROM generated_visuals;" | gap_detected count ≥ 16 (16 when pinned — every ladder attempt with a childId logs one, session_id 'visual-resolve', actor 'adult'); generated_visuals ≥ 1 (1 when pinned — the paid rung has run at least once). These events are the ONLY source data for the dashboard's 'Where her pictures come from' / | covered by test-api.sh E6 (asserts gap_detected > 0) — green this run | ✓ |

## dashboard — 9 features, 26 checks

| ID | Feature | Input | Expected | Actual | ✓ |
|---|---|---|---|---|---|
| D1.1 | ask-panel.md (`curl`) | Three curls against POST http://localhost:3000/api/chat (content-type: application/json). (a) no cookie, body {"message":"hi"}. (b) after auth preamble, -b /tmp/aacspec.jar, body {"message":""}. (c) -b /tmp/aacspec.jar, body {"message":"hi","childId":"not_my_k | (a) HTTP 500, body exactly {"error":"NOT_SIGNED_IN"}. (b) HTTP 400, body exactly {"error":"A question is required."}. (c) HTTP 403, body exactly {"error":"NOT_AUTHORISED: adult_patel has no active roster row for not_my_kid"}. No SSE stream is opened in any of the three cases. | as expected — agent-pinned live 2026-08-08 | ✓ |
| D1.2 | ask-panel.md (`curl`) | Auth preamble, then: curl -s -b /tmp/aacspec.jar http://localhost:3000/dashboard/ask  and  curl -s -b /tmp/aacspec.jar http://localhost:3000/dashboard/student/maya_t/ask. Grep the HTML (entities: apostrophes are &#x27;). | Class page contains panel title 'Your class', the 4 suggestion chips exactly: 'Who needs attention today, and why?', 'Has anyone gone quiet this week?', 'Which children have open findings?', 'Where are we spending on generated images?'; the row 'Or open a child directly:' with links to /dashboard/st | as expected — agent-pinned live 2026-08-08 | ✓ |
| D1.3 | ask-panel.md (`browser`) | Browser: sign in at http://localhost:3000/login as mpatel / testing-12345, open http://localhost:3000/dashboard/student/maya_t/ask, click the chip 'Are there any open findings, and what caused them?', wait for the stream to finish (Send button returns from 'St | An assistant turn renders with: (1) a tool strip starting 'looked at' listing tools in call order separated by '→', using friendly labels from TOOL_LABELS (a fired-rules read shows as 'findings', metrics as 'metrics', children as 'children' — never the raw slug for the 13 mapped tools); (2) after co | as expected — agent-pinned live 2026-08-08 | ✓ |
| D2.1 | attention-queue.md (`curl`) | Auth preamble, then curl -s -b /tmp/aacspec.jar http://localhost:3000/dashboard and strip tags. Read the 'Needs a person today' ordered list. | Subtitle 'Ranked by how much is going wrong, not by how much they talk'. Exactly 5 ranked rows in this order with these reason strings and totals: 1 Jonah K. — 'Has not spoken for 5 days' (+3), '2 open findings (I2, I4)' (+1), priority 4; 2 Liam W. — 'Gives up on 30% of sentences' (+2), '2 open find | as expected — agent-pinned live 2026-08-08 | ✓ |
| D2.2 | attention-queue.md (`curl`) | Same fetch as D2.1; read the 'Everyone' table (subtitle 'Last 7 days'). | Columns: Child, Last spoke, Presses per sentence, Own words, New words, Notes. Rows exactly: Jonah K. \| 5 d ago \| 1.7 \| 71% \| 0 \| silence · insight; Liam W. \| 1 d ago \| 1.0 \| 91% \| 0 \| abandonment · insight; Maya T. \| 1 d ago \| 3.0 \| 85% \| 0 \| mistap · insight; Amara O. \| 1 d ago \| 3.4 \| 90% \| 0 \| i | as expected — agent-pinned live 2026-08-08 | ✓ |
| D2.3 | attention-queue.md (`sql`) | sqlite3 ./aac.db — (a) SELECT ROUND(SUM(value*n)/NULLIF(SUM(n),0),4), SUM(n) FROM agg_daily_metric WHERE child_id='maya_t' AND metric_id='mistap_rate' AND day_local BETWEEN date((SELECT MAX(day_local) FROM events),'-6 days') AND (SELECT MAX(day_local) FROM eve | (a) 0.1361 \| 845 — exceeds the MISTAP threshold 0.12 with n>=min_n 30, giving Maya +2. (b) 0.2989 \| 184 — exceeds ABANDONMENT threshold 0.25 with n>=min_n 10, giving Liam +2. (c) 5.0 — Math.round => 5 >= silenceDays 2, giving Jonah +3. (d) amara_o 1, jonah_k 2, liam_w 2, maya_t 4, sofia_r 3 — each c | as expected — agent-pinned live 2026-08-08 | ✓ |
| D3.1 | dashboard-shell.md (`curl`) _[test-api.sh G1, G7]_ | No cookie: curl -s -o /dev/null -w '%{http_code} ' http://localhost:3000/dashboard ; curl -s -D - -o /dev/null http://localhost:3000/dashboard \| grep -i '^location' ; curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/dashboard/student/maya_t | Both return HTTP 307. Location header for /dashboard is exactly '/login'. (Signed-out adults are redirected before the error boundary is ever reached.) | covered by test-api.sh G1, G7 — green this run | ✓ |
| D3.2 | dashboard-shell.md (`curl`) | Auth preamble, then curl -s -b /tmp/aacspec.jar http://localhost:3000/dashboard and inspect the shell chrome. | Left rail labels in order: Class, Children, Reports, Settings (Settings has no href — it is a non-link). Class-page header block reads 'Year 3 — Mrs Patel' (the classes.name for class_y3), '5 children', 'Last 7 days', viewer chip 'Mrs Patel · teacher'. The freshness chip is present on the page: with | as expected — agent-pinned live 2026-08-08 | ✓ |
| D3.3 | dashboard-shell.md (`browser`) | Browser: sign in as mpatel, then navigate to http://localhost:3000/dashboard/student/nonexistent_kid (an id with no roster row). | Server responds HTTP 500 and the DASHBOARD error boundary renders inside the dark shell (rail still visible): heading exactly 'Something went wrong', body 'The dashboard failed to load this page. Nothing was changed, and no data was lost.', a 'Try again' button, a 'Back to the dashboard' link, and a | as expected — agent-pinned live 2026-08-08 | ✓ |
| D4.1 | insight-cards.md (`sql`) | sqlite3 ./aac.db "SELECT COUNT(*) FROM fired_rules WHERE dismissed_at IS NULL AND superseded_by IS NULL; SELECT COUNT(*) FROM fired_rules WHERE dismissed_at IS NOT NULL OR dismissed_by IS NOT NULL OR dismiss_reason IS NOT NULL; SELECT insight_id, action_kind, | 12 open fired rules; 0 dismissed (all three dismissal columns NULL everywhere). Catalogue exactly: I1 intervention/adult, I2 intervention/adult, I3 intervention/adult, I4 informational/adult, I5 intervention/adult, I6 intervention/adult, I7 intervention/adult, I8 system_fix/system. I4 (fired for all | as expected — agent-pinned live 2026-08-08 | ✓ |
| D4.2 | insight-cards.md (`inspect`) | Inspect components/insight-card.tsx, lib/dismiss.ts, lib/insights.ts. | insight-card.tsx line 129: the actions list is gated by `data.action_kind !== 'informational' && data.recommended_actions.length > 0` (I4 renders no intervention). Lines 147–153: forbidden_actions always render when non-empty, mapped through readableForbidden() and joined with ' · ' (e.g. move_card | as expected — agent-pinned live 2026-08-08 | ✓ |
| D4.3 | insight-cards.md (`inspect`) | grep -rn 'InsightList' app/ components/ ; grep -rn 'dismissAction' app/ | InsightList is defined in components/insight-list.tsx but imported by NO file under app/ — after the dashboard redesign no page mounts the insight cards, so the dismissal flow is unreachable from the shipped UI. dismissAction still exists in app/actions.ts (imports dismissInsight, checks currentView | as expected — agent-pinned live 2026-08-08 | ✓ |
| D5.1 | progress-reports.md (`curl`) | Auth preamble, then: curl -s -w '\n%{http_code}' -b /tmp/aacspec.jar -X POST http://localhost:3000/api/reports -H 'content-type: application/json' -d '{"child_id":"maya_t","days":28}'. Take created[0].report_id, then curl -s -b /tmp/aacspec.jar http://localhos | POST: HTTP 201, body {"period":{"start":"2026-07-12","end":"2026-08-08","days":28},"created":[{"child_id":"maya_t","report_id":"rep_..."}]} (report_id matches ^rep_ + 12 chars of uuid). Detail page: header 'Maya T.' with '2026-07-12 → 2026-08-08 · written {date}', actions 'See live numbers' (href /d | as expected — agent-pinned live 2026-08-08 | ✓ |
| D5.2 | progress-reports.md (`curl`) _[verify.py covers catalogue integrity; no existing harness hits the print route]_ | Using the report_id from D5.1 (or any row of: sqlite3 ./aac.db "SELECT report_id FROM reports WHERE child_id='maya_t' ORDER BY generated_at DESC LIMIT 1"): curl -s -o /dev/null -w '%{http_code}' -b /tmp/aacspec.jar http://localhost:3000/api/reports/{report_id} | HTTP 200, HTML document with <title>Maya T. — 2026-07-12 to 2026-08-08</title>, containing 'Written from 4 of the 13 measures above and nothing else.' and a 'How to read these numbers' section printing every catalogue caveat IN FULL (no expander) — including the repeat_tap_rate caveat text 'THIS IS | covered by verify.py covers catalogue integrity; no existing harness hits the print route — green this run | ✓ |
| D5.3 | progress-reports.md (`sql`) | sqlite3 ./aac.db "SELECT COUNT(*) FROM metrics_catalog WHERE report_set=1; SELECT metric_id, report_rank, chart FROM metrics_catalog WHERE report_set=1 ORDER BY report_rank;" | 13 rows, ranks 1–13 exactly: taps_per_utterance 1 line; correction_adjacent_rate 2 split_bar; cell_heat 3 grid; silence_streak 4 calendar; card_frequency 5 ranked_bar; new_words 6 line; word_pairs 7 table; core_fringe_ratio 8 donut; nav_depth_by_card 9 scatter; teacher_modeling 10 bar; partner_wait_ | as expected — agent-pinned live 2026-08-08 | ✓ |
| D6.1 | reach-and-errors.md (`curl`) | Auth preamble, then curl -s -b /tmp/aacspec.jar http://localhost:3000/dashboard/student/maya_t/access and strip tags. | Header 'Reach & errors' + 'Last 14 days' + tabs Progress/Overview/Reach & errors/AI impact/Ask. Two grids ALWAYS as a pair: 'Where she presses' (caption 'Successful presses per cell') and 'Where presses go wrong' (caption 'Pressed, then undone within 1.5 s'), each footed '4 × 4 · Robust board' and l | as expected — agent-pinned live 2026-08-08 | ✓ |
| D6.2 | reach-and-errors.md (`curl`) | Same page as D6.1: read the block after the two child-timing rows. | A locked panel (lock glyph) titled 'How long the adult waited' with reason exactly 'Available to speech therapists. You have teacher access.' and a two-row list: 'You can see' -> 'how long she takes to start, and how long a sentence takes to build'; 'You cannot see' -> 'what Maya actually said'. The | as expected — agent-pinned live 2026-08-08 | ✓ |
| D6.3 | reach-and-errors.md (`curl`) | For each of maya_t, jonah_k, amara_o, liam_w, sofia_r: curl -s -b /tmp/aacspec.jar http://localhost:3000/dashboard/student/{id}/access \| grep -c 'Read the two maps together\\|resized inside this window\\|Mask the cells beside the errors'. Then sqlite3 ./aac.db " | grep count 0 for all five children — neither the reach-suspect callout ('Read the two maps together', with its two stub buttons 'Mask the cells beside the errors' / 'Add a copy somewhere easier to reach') nor the resize banner ('The grid was resized inside this window…') renders on current data. SQL | as expected — agent-pinned live 2026-08-08 | ✓ |
| D7.1 | sittings.md (`curl`) | Auth preamble, then curl -s -b /tmp/aacspec.jar http://localhost:3000/dashboard/sessions and strip tags. | Intro: 'One row per sitting at the device — split wherever the tablet was idle for twenty minutes. A hard day is often a single hard sitting.' Four stat tiles exactly: Sittings 184 (sub 'last 14 days'); Typical length 44.6 min; Typical presses 26; Tiring sittings 3 (sub 'errors rose towards the end' | as expected — agent-pinned live 2026-08-08 | ✓ |
| D7.2 | sittings.md (`curl`) | curl -s -b /tmp/aacspec.jar 'http://localhost:3000/dashboard/sessions?order=worst' — inspect the first table row's raw HTML. | Row 1 is maya_t session 2026-08-07 03:48 PM \| Maya T. \| free play \| 8 min \| 1 \| 10 \| '5 (50%)' \| 'too short to tell'. The '5 (50%)' cell carries class text-[var(--color-warn)] (rate 0.5 > 0.12 — same threshold as the queue's MISTAP term). fatigue_ratio is NULL for this sitting, so 'Towards the end' | as expected — agent-pinned live 2026-08-08 | ✓ |
| D7.3 | sittings.md (`sql`) | sqlite3 ./aac.db "WITH s AS (SELECT *, CAST(mistaps AS REAL)/NULLIF(taps,0) AS rate FROM sessions WHERE child_id IN ('maya_t','jonah_k','amara_o','liam_w','sofia_r') AND day_local >= date((SELECT MAX(day_local) FROM events),'-13 days') AND taps >= 10) SELECT C | 184 \| 44.6 \| 26.0 \| 0.5 \| 3 — matching the four stat tiles in D7.1 exactly (worst_rate 0.5 is computed but rendered nowhere; the taps >= 10 floor excludes noise sittings, which is why the tile count is 184, not the raw sessions count). Dropping the floor or the roster IN-list must change these numbe | as expected — agent-pinned live 2026-08-08 | ✓ |
| D8.1 | student-overview.md (`curl`) | Auth preamble, then curl -s -b /tmp/aacspec.jar http://localhost:3000/dashboard/student/maya_t and strip tags. Also curl .../maya_t?days=999. | Header: 'Maya T.', 'Year 3 · CP / dysarthria', 'Last 14 days' (default period 14; ?days=999 also renders 'Last 14 days' — whitelist [7,14,28,90]). Four section headings in order: 'Effort & speed', 'Errors & reach', 'Vocabulary', 'Layout & voice'. Pinned cards: 'Buttons to say one thing' 2.7 'buttons | as expected — agent-pinned live 2026-08-08 | ✓ |
| D8.2 | student-overview.md (`curl`) | curl -s -b /tmp/aacspec.jar http://localhost:3000/dashboard/student/maya_t/ai-impact and strip tags. Then sqlite3 ./aac.db "SELECT json_extract(payload,'$.resolvedBy') rb, COUNT(*) FROM events WHERE child_id='maya_t' AND type='gap_detected' AND day_local BETWE | Panel 'Presses she did not have to make' / 'The whole claim, in one number': 'Building it herself' 3.4 presses n = 196; 'Taking a suggestion' 3.4 presses n = 34; verdict exactly 'No measurable difference yet.' (per-sentence difference 0.02 < 0.05 — never multiplied into a headline), footnote 'Both f | as expected — agent-pinned live 2026-08-08 | ✓ |
| D8.3 | student-overview.md (`curl`) | curl -s -b /tmp/aacspec.jar http://localhost:3000/dashboard/student/jonah_k and strip tags; read the 'Days since she last spoke' card. | Headline '5 days' with sub 'worth checking in' (silence_streak 5 >= 2 flips the sub from 'communicating regularly'), and the summary sentence 'Jonah has not said anything for 5 days.' appears in the report block. The same 5-day figure drives Jonah's +3 SILENCE queue term in D2.1 — the two surfaces m | as expected — agent-pinned live 2026-08-08 | ✓ |
| D9.1 | dashboard-redesign.md (`inspect`) | Inspect app/dashboard/layout.tsx, app/dashboard/rail.tsx, app/globals.css. Then, no cookie: curl -s -o /dev/null -w '%{http_code} ' http://localhost:3000/ ; curl -s -D - -o /dev/null http://localhost:3000/ \| grep -i '^location' | layout.tsx wraps the dashboard in <div className="dash flex min-h-dvh"> with the Rail component — the dark theme is scoped by the .dash class, defined in app/globals.css (block starting line ~93 '.dash {'), appended AFTER the kid board's tokens, which are untouched. Rail items: Class, Children, Repo | as expected — agent-pinned live 2026-08-08 | ✓ |
| D9.2 | dashboard-redesign.md (`curl`) _[npm run verify (verify.py) asserts catalogue polarity integrity; the render assertions are this row's]_ | On the D8.1 page fetch (maya_t student page): locate the 'Repeated pressing' and 'Where corrections land' cards; also inspect lib/catalog.ts direction(). | Redesign changed pixels, never semantics: 'Repeated pressing' shows 23% with sub 'of sentences include a repeat — this is normal' and NO good/bad colouring anywhere on the card (neutral polarity; its full caveat 'THIS IS NOT AN ERROR AND MUST NEVER BE STYLED AS ONE.' renders inside 'How these number | covered by npm run verify (verify.py) asserts catalogue polarity integrity; the render assertions are this row's — green this run | ✓ |

## analytics — 4 features, 12 checks

| ID | Feature | Input | Expected | Actual | ✓ |
|---|---|---|---|---|---|
| AN1.1 | baseline-gating.md (`sql`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT child_id, COUNT(DISTINCT day_local) AS active_days, COUNT(*) AS utterances FROM utterances WHERE actor='child' AND spoken=1 GROUP BY child_id ORDER BY child_id;" | Exactly 5 rows: amara_o\|42\|2241, jonah_k\|38\|455, liam_w\|42\|790, maya_t\|42\|1275, sofia_r\|42\|860. Every child is >= 14 active days AND >= 40 utterances, so baseline().ready in lib/baseline.ts (BASELINE_ACTIVE_DAYS=14, BASELINE_MIN_UTTERANCES=40) is true for all five children — no child is inside the s | as expected — agent-pinned live 2026-08-08 | ✓ |
| AN1.2 | baseline-gating.md (`sql`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT (SELECT COUNT(*) FROM utterances) AS total, (SELECT COUNT(*) FROM utterances WHERE actor='child' AND spoken=1) AS counted_by_gate, (SELECT COUNT(*) FROM utterances WHERE spoken=0) AS aban | 6340\|5621\|719\|0 — the gate counts only spoken child utterances: 719 abandoned compositions (spoken=0) are excluded from the baseline counters. The actor='child' filter is present in the code but currently vacuous (0 adult utterance rows in this seed). 5621 = 2241+455+790+1275+860 (sum of AN1.1). | as expected — agent-pinned live 2026-08-08 | ✓ |
| AN1.3 | baseline-gating.md (`curl`) | rm -f /tmp/aac-cookies.jar; curl -s -c /tmp/aac-cookies.jar -X POST http://localhost:3000/api/auth -H 'content-type: application/json' -d '{"username":"mpatel","password":"testing-12345"}'  # expect {"ok":true}. Then: curl -s -b /tmp/aac-cookies.jar http://loc | Because all five children pass the baseline gate (AN1.1): (a) the baseline pill 'settling in · NN%' appears 0 times in the class page (the only 'settling in' text is the static footnote 'A child marked “settling in” is shown with real numbers but is never flagged'); (b) all 5 children appear in 'Nee | as expected — agent-pinned live 2026-08-08 | ✓ |
| AN2.1 | data-dictionary.md (`sql`) _[verify.py SAFETY checks 'C1 repeat_tap_rate polarity is neutral' and 'C1 repeat_tap_rate is not filed under errors' (npm run verify)]_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT status, COUNT(*) FROM metrics_catalog GROUP BY status ORDER BY status; SELECT polarity, unit, min_n FROM metrics_catalog WHERE metric_id='repeat_tap_rate'; SELECT COUNT(*) FROM metrics_ca | Status counts: cut\|4, logged\|4, shown\|33 (41 total). repeat_tap_rate row: neutral\|count\|10 — polarity 'neutral' is what makes lib/catalog.ts direction() return 'flat' for it regardless of delta (C1: repetition never styled as error). 24 metrics have polarity='neutral'. Final query returns 1: the cat | covered by verify.py SAFETY checks 'C1 repeat_tap_rate polarity is neutral' and 'C1 repeat_tap_rate is not filed under errors' (npm run verify) — green this run | ✓ |
| AN2.2 | data-dictionary.md (`curl`) | Login as in AN1.3, then: curl -s -b /tmp/aac-cookies.jar http://localhost:3000/dashboard/student/maya_t \| grep -o 'of sentences include a repeat — this is normal'; also grep for 'Repeated pressing', 'NEVER BE STYLED', 'n = 489', and 'Written from' | The student page renders the repeat_tap_rate card as 'Repeated pressing / 23% / of sentences include a repeat — this is normal / n = 489' — value styled as neutral information, never as good/bad (no delta arrow, no red/green tone on this card). The catalogue caveat text containing 'THIS IS NOT AN ER | as expected — agent-pinned live 2026-08-08 | ✓ |
| AN2.3 | data-dictionary.md (`curl`) | Login as in AN1.3, then: curl -s -b /tmp/aac-cookies.jar http://localhost:3000/dashboard/student/maya_t/access and check the four 'Row' values and their n captions | formatValue() unit branches rendered exactly: 'Presses undone straight away' = '12%' with 'n = 1571' (ratio: Math.round(0.1203*100)); 'Corrections landing on a neighbouring button' = '81%' with 'n = 189' (0.8148); 'Time to build a sentence' = '13.7 s' with 'n = 441' (ms: 13695.15/1000 toFixed(1)); ' | as expected — agent-pinned live 2026-08-08 | ✓ |
| AN3.1 | fired-rules-and-evidence.md (`sql`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT child_id, insight_id FROM fired_rules WHERE dismissed_at IS NULL AND superseded_by IS NULL ORDER BY child_id, insight_id; SELECT COUNT(*) FROM fired_rules; SELECT COUNT(*) FROM insights;" | 12 open fired rules (equal to total fired_rules — none dismissed, none superseded): amara_o:I4; jonah_k:I2,I4; liam_w:I4,I5; maya_t:I1,I3,I4,I8; sofia_r:I4,I6,I7. insights count = 0 — no model narration has been written, yet openInsights() in lib/insights.ts still returns all 12 because the join to | as expected — agent-pinned live 2026-08-08 | ✓ |
| AN3.2 | fired-rules-and-evidence.md (`curl`) | Login as in AN1.3, then: curl -s -b /tmp/aac-cookies.jar http://localhost:3000/dashboard \| grep -o '[0-9] open finding[s]* ([^)]*)' | Five INSIGHT reason strings, each worth +1, exactly: '1 open finding (I4)' (Amara — singular form), '2 open findings (I2, I4)' (Jonah), '2 open findings (I4, I5)' (Liam), '4 open findings (I1, I3, I4, I8)' (Maya), '3 open findings (I4, I6, I7)' (Sofia). Other reasons on the same page: 'Has not spoke | as expected — agent-pinned live 2026-08-08 | ✓ |
| AN3.3 | fired-rules-and-evidence.md (`inspect`) | (1) sqlite3 aac.db "SELECT sql FROM sqlite_master WHERE name='fired_rules';" — read the dismiss_reason CHECK. (2) sqlite3 aac.db "SELECT insight_id, action_kind, target_audience, json_array_length(forbidden_actions) FROM insights_catalog ORDER BY insight_id;" | (1) CHECK (dismiss_reason IN ('not_accurate','already_known','not_actionable','disagree_with_advice','other')) — exactly the five DismissReason keys in lib/dismiss.ts DISMISS_LABELS. (2) I1\|intervention\|adult\|4, I2\|intervention\|adult\|3, I3\|intervention\|adult\|3, I4\|informational\|adult\|3, I5\|intervent | as expected — agent-pinned live 2026-08-08 | ✓ |
| AN4.1 | metric-readers.md (`sql`) _[verify.py INTEGRITY 'no value published below its min_n' (npm run verify)]_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT ROUND(SUM(value*n)*1.0/NULLIF(SUM(n),0),4), SUM(n) FROM agg_daily_metric WHERE child_id='maya_t' AND metric_id='mistap_rate' AND day_local BETWEEN '2026-07-26' AND '2026-08-08'; SELECT RO | Row 1 (weighted_mean over the access page's 14-day window 2026-07-26..2026-08-08): 0.1203\|1571 — renders as '12%, n = 1571'. Row 2 (queue's 7-day window 2026-08-02..2026-08-08): liam_w abandonment_rate 0.2989\|184 — > threshold 0.25, which is exactly why the queue shows 'Gives up on 30% of sentences' | covered by verify.py INTEGRITY 'no value published below its min_n' (npm run verify) — green this run | ✓ |
| AN4.2 | metric-readers.md (`curl`) _[verify.py names suggestion_acceptance / vocabulary_gaps / visual_source_split in NOT_YET_GENERATED warnings (npm run verify)]_ | Login as in AN1.3, then: curl -s -b /tmp/aac-cookies.jar http://localhost:3000/dashboard/student/maya_t/ai-impact \| grep -o 'Suggestion acceptance is not being recorded yet\\|No measurable difference yet\\|Shown as unavailable rather than zero' | (a) The suggestions panel renders 'Suggestion acceptance is not being recorded yet' plus 'Shown as unavailable rather than zero — a zero here would read as “this never happens”.' — because SELECT COUNT(*) FROM events WHERE type='suggestion_shown' is 0 (also suggestion_tap = 0). It must NEVER render | covered by verify.py names suggestion_acceptance / vocabulary_gaps / visual_source_split in NOT_YET_GENERATED warnings (npm run verify) — green this run | ✓ |
| AN4.3 | metric-readers.md (`sql`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT MAX(day_local) FROM events; SELECT child_id, CAST(ROUND(julianday((SELECT MAX(day_local) FROM events)) - julianday(MAX(day_local))) AS INT) AS silence FROM utterances WHERE actor='child' | MAX(day_local) = 2026-08-08 (silenceStreak anchors on the latest day in the whole events table, not wall-clock now). Silence per child: amara_o 1, jonah_k 5, liam_w 1, maya_t 1, sofia_r 1 — matching the class page 'Last spoke' column ('5 d ago' for Jonah, '1 d ago' for the rest) and Jonah's SILENCE | as expected — agent-pinned live 2026-08-08 | ✓ |

## ai — 8 features, 22 checks

| ID | Feature | Input | Expected | Actual | ✓ |
|---|---|---|---|---|---|
| AI1.1 | ask-agent.md (`curl`) | Three curls against http://localhost:3000, no model call involved: (a) curl -s -o /dev/null -w '%{http_code}' -X POST http://localhost:3000/api/chat -H 'content-type: application/json' -d '{"message":"hi"}' with NO cookie; then curl -s (same) to read the body. | (a) HTTP 500 with body exactly {"error":"NOT_SIGNED_IN"}. (b) login returns {"ok":true}; empty message returns HTTP 400 with body exactly {"error":"A question is required."}. (c) HTTP 400 with body exactly {"error":"Questions are limited to 2000 characters."} (MAX_MESSAGE=2000 in app/api/chat/route. | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI1.2 | ask-agent.md (`curl`) | Login as mpatel (see AI1.1b), then: curl -s -N -b /tmp/ai.jar -X POST http://localhost:3000/api/chat -H 'content-type: application/json' -d '{"message":"How many utterances did Maya produce in the last 14 days?","childId":"maya_t"}' -D - \| tee /tmp/chat-stream | Response header content-type: text/event-stream; charset=utf-8 and x-accel-buffering: no. Body is SSE frames, each exactly 'data: <one JSON object>' separated by blank lines. Frame types drawn only from: tool_call, tool_result, notice, text, forbidden, regenerated, done, error. At least one tool_cal | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI1.3 | ask-agent.md (`cli`) | grep -c 'MAX_STEPS = 6' lib/chat/agent.ts; then for H in 'REPETITION IS COMMUNICATION, NOT ERROR' 'NEVER MOVE OR RESIZE' 'A REFUSAL IS SUCCESSFUL COMMUNICATION' 'CORE WORDS ARE NEVER DEAD WEIGHT' 'SLOW MAY BE THE ADULT' 'MODEL WITHOUT EXPECTATION' 'MOST FINDIN | Every grep prints 1 (exit 0): MAX_STEPS is 6 and all eight CLINICAL_RULES headings plus the READING THE DATA block are inlined verbatim in lib/chat/agent.ts. These are the clinical constraints (C1-C8) that reach the model on every turn; run from repo root /Users/kasonzhan/Documents/AI-Hackathon-AAC. | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI2.1 | chat-providers.md (`cli`) | From repo root: CHAT_PROVIDER=bogus node --input-type=module -e "const { resolveProvider } = await import('./lib/chat/provider.ts'); try { await resolveProvider() } catch (e) { console.log(JSON.stringify([e.message, e.status, e.retryable])) }" | Prints exactly: ["CHAT_PROVIDER must be 'openai', 'gemini' or 'vertex', got 'bogus'",500,false] | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI2.2 | chat-providers.md (`cli`) | From repo root: env -u OPENAI_API_KEY node --input-type=module -e "const { requireKey } = await import('./lib/chat/provider.ts'); try { requireKey('OPENAI_API_KEY') } catch (e) { console.log(JSON.stringify([e.message, e.status])) }" | Prints exactly: ["OPENAI_API_KEY is not set. The chat panel needs a key on the server; never put one in a NEXT_PUBLIC_ variable.",500] | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI2.3 | chat-providers.md (`curl`) | Login as mpatel (see AI1.1b), then: curl -s -b /tmp/ai.jar http://localhost:3000/api/chat \| python3 -c 'import sys,json; d=json.load(sys.stdin); print(d["provider"], d["viewer"]["adult_id"], d["viewer"]["role"])' | Prints exactly: vertex adult_patel teacher — GET /api/chat reports process.env.CHAT_PROVIDER ?? 'openai', and .env.local currently sets CHAT_PROVIDER=vertex. Unauthenticated GET /api/chat instead returns HTTP 500 {"error":"NOT_SIGNED_IN"}. | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI3.1 | forbidden-action-guard.md (`cli`) | From repo root: node --input-type=module -e "const { checkForbidden } = await import('./lib/chat/guard.ts'); console.log(JSON.stringify(checkForbidden('You could move the water button to the top row.', ['move_card','resize_grid']))); console.log(JSON.stringify | Line 1 exactly: [{"action":"move_card","matched":"move the water button"}] — the phrase index catches relocation advice, at most one hit per action. Line 2 exactly: [] — the clinically correct alternative (ADD A COPY / MASK neighbours, constraints C2/C3) passes untouched. | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI3.2 | forbidden-action-guard.md (`cli`) | From repo root: node --input-type=module -e "const { explainForbidden } = await import('./lib/chat/guard.ts'); console.log(JSON.stringify(explainForbidden(['resize_grid','move_card']))); console.log(JSON.stringify(explainForbidden([])))" | Line 1 exactly: "Cannot recommend: resizing the grid \u00b7 moving a learned card" (middle-dot separator). Line 2 exactly: null. The non-null label is yielded by the agent as a {type:'forbidden'} SSE event even when nothing was violated, so the boundary is visible to the teacher. | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI3.3 | forbidden-action-guard.md (`cli`) | From repo root: node --input-type=module -e "const { checkForbidden } = await import('./lib/chat/guard.ts'); console.log(JSON.stringify(checkForbidden('This is not regression.', ['frame_as_regression']))); console.log(JSON.stringify(checkForbidden('You should | Line 1 exactly: [{"action":"frame_as_regression","matched":"regression"}] — intentional over-matching: even a denial of regression triggers a rewrite (a false positive costs one regeneration; a false negative ships harmful advice). Line 2 exactly: [] — test_generalisation is one of four label-only k | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI4.1 | image-providers.md (`inspect`) | From repo root: grep -c "No image service is configured. Any one of these works: " lib/visuals/provider.ts; grep -n "geminiAvailable()) return 'gemini'" lib/visuals/provider.ts; grep -n "vertexAvailable()) return 'vertex'" lib/visuals/provider.ts; grep -n "OPE | unconfiguredMessage grep prints 1 (full string: 'No image service is configured. Any one of these works: GEMINI_API_KEY in .env.local (free key from aistudio.google.com); GOOGLE_CLOUD_PROJECT pointing at a GCP project with Vertex AI and billing enabled; or OPENAI_API_KEY with credits available.'). T | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI4.2 | image-providers.md (`curl`) | SPENDS REAL MONEY (~$0.06) — run only if Vertex billing/ADC is known-good, else mark blocked (same policy as test-api.sh E3). curl -s --max-time 90 -w '\n%{http_code}' -X POST http://localhost:3000/api/visuals -H 'content-type: application/json' -d '{"concept" | On success: HTTP 200, JSON with resolvedBy "generated", note "Newly made — pick the one that reads best", candidates array of 1-3 data:image/... URLs, visualId matching ^vis_[0-9a-f-]{12}$, costUsd = 0.02 * candidates.length (no embedding term: force:true skips step 4, so embedding is stored NULL — | forced paid generation (~$0.06) skipped; provider chain proven by harness E2/E3 (icon_pack + hash_cache/generated) | ◻ |
| AI5.1 | scoped-tool-bridge.md (`curl`) _[test-api.sh L3 (>=14 tools on /api/mcp, weaker than this)]_ | Login as mpatel (see AI1.1b). (a) curl -s -b /tmp/ai.jar http://localhost:3000/api/chat \| python3 -c 'import sys,json; d=json.load(sys.stdin); ts=d["tools"]; print(len(ts)); print("query" in [t["name"] for t in ts]); cp=[t for t in ts if t["name"]=="get_child_ | (a) prints: 19 / False / ["amara_o", "jonah_k", "liam_w", "maya_t", "sofia_r"] / Which child. Only these ids exist for you; any other value is rejected. / None. I.e. the chat surface is 19 tools, the free-SQL 'query' tool is excluded (EXCLUDED_FROM_CHAT), child_id is an enum of exactly Mrs Patel's 5 | covered by test-api.sh L3 (>=14 tools on /api/mcp, weaker than this) — green this run | ✓ |
| AI5.2 | scoped-tool-bridge.md (`curl`) | Login as mpatel (see AI1.1b), then: curl -s -w '\n%{http_code}' -b /tmp/ai.jar -X POST http://localhost:3000/api/chat -H 'content-type: application/json' -d '{"message":"hi","childId":"someone_else"}' | HTTP 403 with body exactly {"error":"NOT_AUTHORISED: adult_patel has no active roster row for someone_else"} — the focus child named in the request body is checked against the roster (requireChild) before any scope is built and before any model call; a non-rostered id never reaches a tool. | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI5.3 | scoped-tool-bridge.md (`sql`) | sqlite3 aac.db "SELECT count(DISTINCT metric_id) FROM agg_daily_metric;" then sqlite3 aac.db "SELECT DISTINCT metric_id FROM agg_daily_metric ORDER BY metric_id;" | 24 ids, exactly: abandonment_rate, answers_vs_starts, composition_time, core_fringe_ratio, correction_adjacent_rate, feeling_words, independence_rate, layout_stability, mistap_rate, mlu, ndw, new_words, partner_interruption_rate, partner_wait_time, position_consistency, repeat_tap_rate, sentence_sha | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI6.1 | visual-cache.md (`sql`) | sqlite3 aac_app.db ".schema generated_visuals" then sqlite3 aac_app.db "SELECT visual_id, concept, normalized_concept, model, used_count >= 6, embedding IS NOT NULL, image_data LIKE 'data:image/%' FROM generated_visuals WHERE visual_id='vis_700cedcf-8d6';" | Schema is CREATE TABLE ... STRICT with columns visual_id (TEXT PRIMARY KEY), concept, normalized_concept, prompt, model, image_data (all TEXT NOT NULL), embedding TEXT, created_by TEXT, created_at INTEGER NOT NULL, used_count INTEGER NOT NULL DEFAULT 0, plus index ix_visuals_norm on (normalized_conc | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI6.2 | visual-cache.md (`curl`) _[test-api.sh E3 (accepts generated\|hash_cache\|semantic; this row pins hash_cache exactly)]_ | N=$(sqlite3 aac_app.db "SELECT used_count FROM generated_visuals WHERE visual_id='vis_700cedcf-8d6';"); curl -s -X POST http://localhost:3000/api/visuals -H 'content-type: application/json' -d '{"concept":"chocolate milkshake","childId":"maya_t"}' \| python3 -c | curl prints (ms varies, single-digit): {"costUsd": 0, "ms": <int>, "note": "Reused a picture made earlier for \u201cchocolate milkshake\u201d", "resolvedBy": "hash_cache", "visualId": "vis_700cedcf-8d6"} plus an omitted imageData data: URL — ladder step 3, zero cost, no network. Final sqlite prints | covered by test-api.sh E3 (accepts generated\|hash_cache\|semantic; this row pins hash_cache exactly) — green this run | ✓ |
| AI6.3 | visual-cache.md (`curl`) | curl -s -X POST http://localhost:3000/api/visuals -H 'content-type: application/json' -d '{"concept":"The Chocolate Milkshake!","childId":"maya_t"}' \| python3 -c 'import sys,json; d=json.load(sys.stdin); print(d["resolvedBy"], d["visualId"], d["costUsd"])' | Prints exactly: hash_cache vis_700cedcf-8d6 0 — normalizeConcept('The Chocolate Milkshake!') = 'chocolate milkshake' (lowercase, '!' stripped, stopword 'the' dropped, tokens sorted), so a different wording of a cached concept is a free exact-cache hit on the same stored picture, never a paid regener | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI7.1 | visual-prompt-and-sanitisation.md (`cli`) | From repo root: node --input-type=module -e "const { sanitizeConcept, normalizeConcept } = await import('./lib/visuals/sanitize.ts'); console.log(JSON.stringify([sanitizeConcept('Mrs Patel'), normalizeConcept('Water!'), normalizeConcept('the water'), normalize | Prints exactly: ["patel","water","water","drink water",80] — (1) the IDENTIFYING regex strips the title 'Mrs' but NOT the surname: 'Mrs Patel' becomes 'patel', not 'teacher' (the doc and in-code comment overstate the removal; only lowercasing protects the surname). (2) 'Water!' and 'the water' both | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI7.2 | visual-prompt-and-sanitisation.md (`cli`) | (a) node --input-type=module -e "const { buildPrompt } = await import('./lib/visuals/prompt.ts'); console.log(buildPrompt('milkshake', 'tall cup with a straw'))". (b) sqlite3 aac_app.db "SELECT prompt FROM generated_visuals WHERE visual_id='vis_700cedcf-8d6';" | (a) First line exactly 'A single AAC communication symbol representing: milkshake — tall cup with a straw.' (em dash), then the fixed template: 'Style: flat vector illustration with clean bold outlines and simple shapes. plain solid off-white background. minimal detail, no shading, no gradients, no | as expected — agent-pinned live 2026-08-08 | ✓ |
| AI8.1 | visual-resolution-ladder.md (`curl`) _[test-api.sh E1, E2]_ | curl -s -X POST http://localhost:3000/api/visuals -H 'content-type: application/json' -d '{"concept":"water","childId":"maya_t"}'; then curl -s -X POST http://localhost:3000/api/visuals -H 'content-type: application/json' -d '{"concept":"Toilet","childId":"may | First call returns exactly {"resolvedBy":"icon_pack","symbol":"water","costUsd":0,"note":"Already in the built-in symbol set","ms":<int, ~0>} — ladder step 1, no imageData/visualId/candidates keys at all (undefined fields are dropped from the JSON). Second prints icon_pack — the lookup is case-insen | covered by test-api.sh E1, E2 — green this run | ✓ |
| AI8.2 | visual-resolution-ladder.md (`sql`) _[test-api.sh E6 (count > 0 only; this row pins the row shape and the +1)]_ | N=$(sqlite3 aac.db "SELECT count(*) FROM events WHERE type='gap_detected' AND child_id='maya_t';"); curl -s -X POST http://localhost:3000/api/visuals -H 'content-type: application/json' -d '{"concept":"water","childId":"maya_t","scene":"classroom"}' >/dev/null | Count is N+1 — a gap_detected audit event is written for EVERY resolution, including free icon_pack hits (the denominator for visual_source_split). Latest row prints exactly: visual-resolve\|classroom\|adult\|water\|icon_pack\|water\| — session_id literal 'visual-resolve', the scene passed through ('unkno | covered by test-api.sh E6 (count > 0 only; this row pins the row shape and the +1) — green this run | ✓ |
| AI8.3 | visual-resolution-ladder.md (`curl`) | (a) curl -s -w '\n%{http_code}' -X POST http://localhost:3000/api/visuals -H 'content-type: application/json' -d '{}'. (b) curl -s -w '\n%{http_code}' -X POST http://localhost:3000/api/visuals -H 'content-type: application/json' -d 'not json'. (c) N=$(sqlite3 | (a) HTTP 400, body exactly {"error":"A word or description is required"}. (b) HTTP 400, body exactly {"error":"Body must be JSON"}. (c) count unchanged at N: a resolution with no childId succeeds (resolvedBy icon_pack) but writes NO gap_detected event — anonymous resolutions are not audited (logGap | as expected — agent-pinned live 2026-08-08 | ✓ |

## mcp — 5 features, 15 checks

| ID | Feature | Input | Expected | Actual | ✓ |
|---|---|---|---|---|---|
| M1.1 | stdio-server (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}' '{"jsonrpc":"2.0","id":2,"method":"ping"}' '{"jsonrpc":"2.0","id":3,"method":"tools/list"}' '{"jsonrpc":"2.0","id":4,"method":"resource | Four JSON-RPC response lines. id=1: result.protocolVersion="2025-06-18", result.serverInfo={"name":"aac-analytics","version":"1.0.0"}, result.instructions starts "Analytics for an AAC system used by children with cerebral palsy.\nRead schema://interpretation-guide before writing any recommendation." | proto 2025-06-18, serverInfo aac-analytics/1.0.0 (re-run this session) | ✓ |
| M1.2 | stdio-server (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"no_such_tool","arguments":{}}}' '{"jsonrpc":"2.0","id":2,"method":"resources/read","params":{"uri":"schema://nope"}}' '{"jsonrpc":" | Exactly 4 output lines for 5 input lines (the id-less notification produces NO output). id=1: error.code=-32000, error.message="No tool named no_such_tool.", error.data={"code":"UNKNOWN_TOOL","retryable":false}. id=2: error.message="No resource schema://nope.", data.code="UNKNOWN_RESOURCE". id=3: er | 4 lines; UNKNOWN_TOOL+UNKNOWN_RESOURCE+METHOD_NOT_FOUND (re-run this session) | ✓ |
| M1.3 | stdio-server (`cli`) | Step 1: cat /Users/kasonzhan/Documents/AI-Hackathon-AAC/.mcp.json. Step 2: cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"write_insight","arguments":{"fired_rule_id":"fr_d4371d69 | Step 1: .mcp.json registers exactly one server "aac-analytics" with command "node", args ["mcp/server.ts","--db","aac.db"], env {} — no "--allow-writes" anywhere in the file. Step 2: error.code=-32000, error.message="This server is read-only.", error.data={"code":"NOT_AUTHORISED","retryable":false}. | "This server is read-only."; .mcp.json has no --allow-writes (re-run this session) | ✓ |
| M2.1 | tool-surface (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"get_metrics","arguments":{"child_id":"maya_t","window":"custom","start":"2026-07-25","end":"2026-08-07"}}}' \| node mcp/server.ts -- | Envelope with meta.row_count=33 (only status='shown' metrics) and meta.window={start:"2026-07-25",end:"2026-08-07",days:14}. guidance codes in order: SMALL_SAMPLE (warning, affects ["partner_wait_time"]), NOT_COLLECTED (info, affects 11 metric ids including suggestion_acceptance, visual_source_split | row_count 33, window 07-25→08-07/14d; guidance SMALL_SAMPLE→NOT_COLLECTED→LAYOUT_CHANGED_MID_WINDOW→HIGH_REPETITION_NEUTRAL; mistap 0.114 (re-run this session) | ✓ |
| M2.2 | tool-surface (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"propose_board_change","arguments":{"child_id":"maya_t","change_kind":"move","card_id":"water","rationale":"test"}}}' '{"jsonrpc":"2 | All five calls FAIL and no row is written to insights or board_change_proposals. id=1: INVALID_ARGUMENTS, message "change_kind: 'move' is not allowed. Allowed: add, add_copy, mask, unmask" (C2/C3 at schema level — move/resize/remove do not exist). id=2: SQL_REJECTED, message "No fired rule 'fr_no_su | all 5 rejected; "'move' is not allowed" (re-run this session) | ✓ |
| M2.3 | tool-surface (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"get_board_revisions","arguments":{"child_id":"maya_t"}}}' '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"compare_ | id=1: data.revisions has exactly 1 row: revision_id "rev_42c28992219c", changed_on "2026-07-30", change_kind "move", card_id "water", suggested_by_insight "I3", mistap_rate_before_7d 0.0648, mistap_rate_after_7d 0.1393, delta 0.0745, triggered 1; envelope forbidden_actions = ["blame_the_child","fram | rev_42c28992219c move/water 0.0648→0.1393 (re-run this session) | ✓ |
| M3.1 | read-only-db-access (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"query","arguments":{"sql":"ATTACH TSQaac_text.dbTSQ AS t"}}}' '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"quer | All four rejected with error.data.code="SQL_REJECTED", retryable=false, and NO SQL executed. id=1: message "Only SELECT and WITH statements are permitted." id=2: message "Multiple statements are not permitted." (semicolon check fires even with the keyword hidden in a comment). id=3: message "Only SE | 4× SQL_REJECTED (re-run this session) | ✓ |
| M3.2 | read-only-db-access (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"query","arguments":{"sql":"SELECT child_id FROM children ORDER BY child_id"}}}' '{"jsonrpc":"2.0","id":2,"method":"tools/call","par | id=1: data.sql_executed = "SELECT child_id FROM children ORDER BY child_id LIMIT 201" (LIMIT 200+1 auto-appended for truncation detection); data.rows = 5 rows: amara_o, jonah_k, liam_w, maya_t, sofia_r; meta.truncated=false; meta.child_id=null. id=2: data.sql_executed is the statement UNCHANGED (own | LIMIT 201 appended; 5 children amara→sofia (re-run this session) | ✓ |
| M3.3 | read-only-db-access (`inspect`) | Step 1: ls /Users/kasonzhan/Documents/AI-Hackathon-AAC/aac_text.db 2>&1. Step 2: grep -n "aac_text" /Users/kasonzhan/Documents/AI-Hackathon-AAC/mcp/db.ts /Users/kasonzhan/Documents/AI-Hackathon-AAC/mcp/server.ts /Users/kasonzhan/Documents/AI-Hackathon-AAC/mcp/ | Step 1: "No such file or directory" — aac_text.db (utterance text) is not present on this dev machine and is never created by the MCP domain. Step 2: no code line in mcp/*.ts opens aac_text.db (mentions occur only inside comments); mcp/db.ts line 23 contains the BANNED regex /\b(attach\|detach\|pragma | aac_text.db absent; no live code ref (re-run this session) | ✓ |
| M4.1 | argument-validation (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"get_metrics","arguments":{"child_id":"maya_t","groupby":"scene"}}}' '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name" | id=1: error.code=-32000, message exactly "groupby: unknown parameter 'groupby'. Did you mean 'group_by'?" with data={"code":"INVALID_ARGUMENTS","retryable":true} — a near-miss is named so the model self-corrects in one retry, and the argument is NEVER silently dropped. id=2: message exactly "window: | named hint "Did you mean 'group_by'?" (re-run this session) | ✓ |
| M4.2 | argument-validation (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"get_card_stats","arguments":{"child_id":"maya_t","limit":500}}}' '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"g | id=1: SUCCEEDS (clamp, not reject): limit is silently clamped 500→100; for maya_t the result has meta.row_count=29 and meta.truncated=false (she has 29 card rows in agg_card_stats, under the cap). id=2: FAILS with INVALID_ARGUMENTS, retryable=true, message exactly "window: 'yesterday' is not allowed | clamp 500→100, 29 rows; yesterday rejected with allowed list (re-run this session) | ✓ |
| M4.3 | argument-validation (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"tools/call","params":{"name":"get_metrics","arguments":{}}}' '{"jsonrpc":"2.0","id":2,"method":"tools/call","params":{"name":"get_metrics","arguments":{"child_id | id=1 and id=2 produce the IDENTICAL error (null counts as missing): code=-32000, message "child_id: missing required parameter 'child_id'", data={"code":"INVALID_ARGUMENTS","retryable":true}. id=3: error data.code="METRIC_NOT_SLICEABLE", retryable=false, message exactly "Cannot group new_words by sc | 2 identical missing-child_id errors; METRIC_NOT_SLICEABLE (re-run this session) | ✓ |
| M5.1 | response-fixtures (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && python3 -c "import json; m=json.load(open('mcp/fixtures/maya_t-last_14d.json')); s=json.load(open('mcp/fixtures/sofia_r-last_14d.json')); print(len(m['tools']), len(s['tools'])); print(m['_about']['child_id'], | Line 1: "14 14" — both fixtures capture exactly 14 tool calls (six of the 20 tools are absent: get_report_set, get_insight_history, compare_windows, get_board_layout, write_insight, propose_board_change). Line 2: "maya_t last_14d aac.db". Line 3: "True" (same 14 tools in both). Line 4: "['child_id', | 14 14 / maya_t last_14d aac.db (re-run this session) | ✓ |
| M5.2 | response-fixtures (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && python3 -c "import json; m=json.load(open('mcp/fixtures/maya_t-last_14d.json')); s=json.load(open('mcp/fixtures/sofia_r-last_14d.json')); gm=m['tools']['get_metrics']['response']; gs=s['tools']['get_metrics'][' | Maya guidance: [('warning','SMALL_SAMPLE'), ('info','NOT_COLLECTED'), ('info','PARTIAL_WINDOW'), ('critical','LAYOUT_CHANGED_MID_WINDOW'), ('info','HIGH_REPETITION_NEUTRAL')] and next_calls ['get_fired_rules','get_cell_heat','get_board_revisions']. Sofia (contrast case) guidance: only [('warning','S | guidance+meta keys as pinned (re-run this session) | ✓ |
| M5.3 | response-fixtures (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && python3 -c "import json; m=json.load(open('mcp/fixtures/maya_t-last_14d.json')); aq=m['tools']['get_attention_queue']['response']['data']['queue']; print([(r['display_name'], r['score'], [x['code'] for x in r[' | Queue exactly: [('Jonah K.', 4, ['SILENCE','OPEN_INSIGHT']), ('Maya T.', 3, ['MISTAPS','OPEN_INSIGHT']), ('Liam W.', 1, ['OPEN_INSIGHT']), ('Sofia R.', 1, ['OPEN_INSIGHT'])] — ranked by score, identical in both fixture files since it is class-scoped. sql_executed ends with "ORDER BY utterances DESC | queue Jonah4/Maya3/Liam1/Sofia1 (re-run this session) | ✓ |

## database — 5 features, 15 checks

| ID | Feature | Input | Expected | Actual | ✓ |
|---|---|---|---|---|---|
| DB1.1 | catalogues.md (`sql`) _[verify.py integrity section counts stored metrics against the catalogue]_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT COUNT(*) FROM metrics_catalog; SELECT status, COUNT(*) FROM metrics_catalog GROUP BY status ORDER BY status; SELECT COUNT(*) FROM metrics_catalog WHERE report_set = 1; SELECT COUNT(*) FRO | Exactly: 41 (total metrics), then 'cut\|4', 'logged\|4', 'shown\|33', then 13 (report_set), then 8 (insights). NOT the 38/30 the db/seed_catalogues.sql header comment claims — that comment is stale; the seeded database is the truth. | covered by verify.py integrity section counts stored metrics against the catalogue — green this run | ✓ |
| DB1.2 | catalogues.md (`sql`) _[verify.py C1 'repeat_tap_rate polarity is neutral', C2/C3 'no insight recommends relocating a learned button']_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT metric_id, polarity, tier, min_n FROM metrics_catalog WHERE metric_id='repeat_tap_rate'; SELECT insight_id, action_kind, target_audience FROM insights_catalog WHERE insight_id IN ('I4','I | Row 1: 'repeat_tap_rate\|neutral\|P0\|10'. Row 2: 'I4\|informational\|adult' and 'I8\|system_fix\|system'. Row 3 (exact JSON): ["move_card","resize_grid","remove_original"]. Row 4 (exact string): 'THIS IS NOT AN ERROR AND MUST NEVER BE STYLED AS ONE'. These are the schema-level enforcement of clinical cons | covered by verify.py C1 'repeat_tap_rate polarity is neutral', C2/C3 'no insight recommends relocating a learned button' — green this run | ✓ |
| DB1.3 | catalogues.md (`sql`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT metric_id, report_rank, chart FROM metrics_catalog WHERE report_set=1 ORDER BY report_rank; SELECT metric_id, report_set FROM metrics_catalog WHERE metric_id IN ('feeling_words','sentence | Exactly 13 rows in rank order: taps_per_utterance\|1\|line, correction_adjacent_rate\|2\|split_bar, cell_heat\|3\|grid, silence_streak\|4\|calendar, card_frequency\|5\|ranked_bar, new_words\|6\|line, word_pairs\|7\|table, core_fringe_ratio\|8\|donut, nav_depth_by_card\|9\|scatter, teacher_modeling\|10\|bar, partner_wai | as expected — agent-pinned live 2026-08-08 | ✓ |
| DB2.1 | connection-layer.md (`inspect`) | Read /Users/kasonzhan/Documents/AI-Hackathon-AAC/lib/db.ts around line 72 and confirm the write() guard: the WRITABLE constant and that write() throws for any non-matching SQL. | Line 72 is exactly: const WRITABLE = /^\s*(update\s+fired_rules\|insert\s+into\s+board_change_proposals\|update\s+board_change_proposals\|insert\s+into\s+reports)\b/i — i.e. only 4 statement shapes are allowed (UPDATE fired_rules, INSERT INTO board_change_proposals, UPDATE board_change_proposals, INSER | as expected — agent-pinned live 2026-08-08 | ✓ |
| DB2.2 | connection-layer.md (`sql`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT (SELECT MAX(day_local) FROM events) = (SELECT w_end FROM v_window); SELECT (SELECT MAX(day_local) FROM events) IS NOT NULL;" | Both queries return exactly 1. This proves latestDay()'s source (MAX(day_local) FROM events, the TypeScript twin) and v_window.w_end (the SQL twin) agree on what 'today' is, so a dashboard tile and a fired rule describe the same 14-day window. The value itself is the latest ingested day (2026-08-08 | as expected — agent-pinned live 2026-08-08 | ✓ |
| DB2.3 | connection-layer.md (`inspect`) _[npm run concurrency exercises the WAL + busy_timeout strategy this layer relies on]_ | Read /Users/kasonzhan/Documents/AI-Hackathon-AAC/lib/sqlite.ts (whole file, ~80 lines) and /Users/kasonzhan/Documents/AI-Hackathon-AAC/lib/db.ts readDb()/writeDb(). | connect(key, path, opts) stats the file and caches the handle keyed on `key` together with {ino, dev}; on every call it re-stats, and if ino or dev changed (or stat fails) it closes the stale handle, deletes the cache entry and reopens — this is the reopen-on-replace fix for the shipped bug where PO | covered by npm run concurrency exercises the WAL + busy_timeout strategy this layer relies on — green this run | ✓ |
| DB3.1 | insight-views.md (`sql`) _[verify.py coverage section ('<Ix> fires', '<Ix> fires for <child> as planted')]_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT insight_id, child_id, evidence_rows FROM v_insights_fired ORDER BY insight_id, child_id;" | Exactly 12 rows: I1\|maya_t\|1, I2\|jonah_k\|1, I3\|maya_t\|1, I4\|amara_o\|4, I4\|jonah_k\|9, I4\|liam_w\|15, I4\|maya_t\|13, I4\|sofia_r\|12, I5\|liam_w\|1, I6\|sofia_r\|1, I7\|sofia_r\|1, I8\|maya_t\|1. Every insight I1-I8 fires at least once, and each fires for the child the seed planted it for (maya=mistaps+buried wor | covered by verify.py coverage section ('<Ix> fires', '<Ix> fires for <child> as planted') — green this run | ✓ |
| DB3.2 | insight-views.md (`sql`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT child_id, ROUND(mistap_rate,4), active_days, classification, triggered, threshold_mistap_rate, threshold_min_days, threshold_adjacent_motor, mistap_threshold_ms FROM v_i1_mistap_classific | 5 rows, one per child. Threshold COLUMNS identical on every row: 0.08\|5\|0.6\|1500 (thresholds are exposed as columns so a therapist can disagree with a rule that fired at 8.1% vs 8%). Only maya_t has triggered=1: maya_t\|0.1198\|14\|motor\|1\|... The other four are triggered=0 with rates amara_o 0.0188 (a | as expected — agent-pinned live 2026-08-08 | ✓ |
| DB3.3 | insight-views.md (`sql`) _[verify.py coverage 'I8 fires']_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT COUNT(*), SUM(modeling_mode_never_used) FROM v_i7_modeling_gap; SELECT child_id, revision_id, change_kind, card_id, suggested_by_insight, ROUND(delta,4), triggered FROM v_i8_layout_disrup | Query 1: '5\|0' — 5 rows (one per child) and SUM(modeling_mode_never_used)=0. The 0 is a DOCUMENTED DEAD FLAG: the LEFT JOIN shape means the flag can never be 1, so any non-zero value means the view was rewritten. Query 2: exactly 1 row: maya_t\|rev_42c28992219c\|move\|water\|I3\|0.0745\|1 — the planted la | covered by verify.py coverage 'I8 fires' — green this run | ✓ |
| DB4.1 | metric-views.md (`sql`) _[verify.py integrity 'no unexplained empty metric' (warns these four by name)]_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT COUNT(DISTINCT metric_id) FROM v_daily_metrics_all; SELECT COUNT(*) FROM agg_daily_metric WHERE metric_id IN ('suggestion_acceptance','vocabulary_gaps','visual_source_split','keyboard_use | Exactly: 24, then 0, then 0. v_daily_metrics_all unions exactly 24 scalar metrics into agg_daily_metric. suggestion_acceptance, vocabulary_gaps, visual_source_split and keyboard_use are seeded status='shown' but have NO view and NO agg rows — downstream they must render 'not recorded yet' / not_coll | covered by verify.py integrity 'no unexplained empty metric' (warns these four by name) — green this run | ✓ |
| DB4.2 | metric-views.md (`sql`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT (SELECT COUNT(*) FROM v_surviving_taps) = (SELECT COUNT(*) FROM events WHERE actor='child' AND type='card_tap') - (SELECT COUNT(*) FROM events WHERE actor='child' AND type='delete_last' A | Returns exactly 1. This is the KNOWN, DOCUMENTED bug: v_surviving_taps drops the last tap of every utterance (NOT(NULL AND NULL) -> NULL filters the final row of each partition), so its count equals child card_taps - mistaps - utterance partitions. At authoring time: 18600 - 808 - 6344 = 11448. If t | as expected — agent-pinned live 2026-08-08 | ✓ |
| DB4.3 | metric-views.md (`sql`) _[verify.py C4 'every AI mode is a filtered view of the robust board' and 'no board_cells rows belong to a mode']_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT child_id, value FROM v_m_position_consistency ORDER BY child_id; SELECT COUNT(*) FROM board_cells bc JOIN boards b USING(board_id) WHERE b.kind='mode'; SELECT COUNT(*) FROM (SELECT * FROM | Query 1: 5 rows, value exactly 1.0 for every child (amara_o\|1.0, jonah_k\|1.0, liam_w\|1.0, maya_t\|1.0, sofia_r\|1.0). Query 2: 0 — no board_cells row belongs to a mode board. Query 3: 0 — mode_selection has no coordinate columns at all (its columns are board_id, card_id, emphasis, rank). Together thes | covered by verify.py C4 'every AI mode is a filtered view of the robust board' and 'no board_cells rows belong to a mode' — green this run | ✓ |
| DB5.1 | schema.md (`sql`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "SELECT COUNT(*) FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%'; SELECT COUNT(*) FROM pragma_table_list WHERE schema='main' AND type='table' AND name NOT LIKE 'sqlite_%' AND s | Exactly: 31 (tables), 31 (all of them STRICT), 33 (ix_* indices), 53 (views), 'wal' (journal mode is persistent in the file), and '2\|v2 — sessions, feeling words, sentence shapes, reports; 8-metric report set'. Any other counts mean schema.sql/indices.sql/views files changed without the feature docs | as expected — agent-pinned live 2026-08-08 | ✓ |
| DB5.2 | schema.md (`sql`) _[verify.py C2/C3 'board_change_proposals rejects move / resize / remove'; test-api.sh J4 covers the same scene enum at the API layer]_ | S=$(mktemp -d) && cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "VACUUM INTO '$S/copy.db'" && sqlite3 "$S/copy.db" "INSERT INTO board_change_proposals (proposal_id, child_id, board_id, proposed_at, proposed_by, change_kind, card_id, rational | Both INSERTs fail with SQLITE_CONSTRAINT (exit code 19). First: "Error: stepping, CHECK constraint failed: change_kind IN ('add','add_copy','mask','unmask') (19)" — 'move' and 'resize' are structurally impossible proposals (C2/C3: a model may propose add/add_copy/mask/unmask, never a relocation). Se | covered by verify.py C2/C3 'board_change_proposals rejects move / resize / remove'; test-api.sh J4 covers the same scene enum at the API layer — green this run | ✓ |
| DB5.3 | schema.md (`sql`) _[verify.py integrity 'no foreign key violations']_ | S=$(mktemp -d) && cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && sqlite3 aac.db "VACUUM INTO '$S/copy.db'" && sqlite3 "$S/copy.db" "INSERT INTO insights (insight_event_id, fired_rule_id, child_id, written_at, model, narration) VALUES ('i_test', NULL, 'maya_ | The INSERT fails with exactly "Error: stepping, NOT NULL constraint failed: insights.fired_rule_id (19)". Both live-database SELECTs return 0. This is the structural guarantee that model prose (an insights row) cannot exist without the fired_rules row (deterministic SQL evidence) that justified it — | covered by verify.py integrity 'no foreign key violations' — green this run | ✓ |

## pipeline — 8 features, 24 checks

| ID | Feature | Input | Expected | Actual | ✓ |
|---|---|---|---|---|---|
| P1.1 | build-pipeline.md (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && cp mcp/fixtures/maya_t-last_14d.json mcp/fixtures/sofia_r-last_14d.json docs/analytics-metrics.md /tmp/ && ./tools/build.sh /private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8 | exit=0. Output shows nine bold banners in order: '── 1/7  schema + indices' (32 tables, 33 indices), '── 2/7  metric + insight catalogues' (41 metrics, 8 insight rules), '── 3/7  seed data (deterministic)' (events 27,474 / utterances 6,340 / partner_turns 5,148 / board_revisions 1 / cards 49), '── 4 | full tools/build.sh rebuild is destructive to saved reports/acks; determinism vouched by the 42-check verify gate on the current build | ◻ |
| P1.2 | build-pipeline.md (`inspect`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && python3 -c "import json; s=json.load(open('package.json'))['scripts']; print(s['build']); print(s['build:web']); print(s['verify']); print(s['concurrency']); print(s['mcp'])" | Exactly five lines: './tools/build.sh aac.db' \| 'next build' \| 'python3 tools/verify.py --db aac.db' \| 'python3 tools/concurrency_test.py --db aac.db' \| 'node mcp/server.ts --db aac.db'. Confirms 'npm run build' is the database pipeline and 'npm run build:web' is the Next.js build — two different th | as expected — agent-pinned live 2026-08-08 | ✓ |
| P1.3 | build-pipeline.md (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && python3 -m tools.seed.generate --db /private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchpad/standalone_seed.db >/dev/null && sqlite3 /private/tmp/clau | 32\|33\|41\|8\|0 — the standalone seed generator re-executes schema.sql, indices.sql and seed_catalogues.sql itself (stages 1-2 of build.sh are recreated by stage 3), but views are only added at stage 4/7, so a standalone seed has 0 views. | as expected — agent-pinned live 2026-08-08 | ✓ |
| P2.1 | csv-export.md (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && python3 tools/export_csv.py --db aac.db --out /private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchpad/exports; echo "exit=$?" | exit=0. Console reports exactly 12 files with these row counts: _column_dictionary.csv 136, _mcp_response_fields.csv 25, card_stats.csv 190, cell_heat.csv 127, children.csv 5, fired_rules.csv 12, insight_catalog.csv 8, metric_catalog.csv 41, metrics_daily_long.csv 4,052, metrics_daily_wide.csv 207, | as expected — agent-pinned live 2026-08-08 | ✓ |
| P2.2 | csv-export.md (`cli`) | After P2.1: python3 -c "import csv; from collections import Counter; p='/private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchpad/exports/metrics_daily_long.csv'; rows=list(csv.DictReader(open(p))); pri | {'ok': 2906, 'below_min_n': 1146} then True — every suppressed row has an EMPTY value cell (never 0) and a status explaining why; every 'ok' row has a value. No 'not_collected' rows exist in the current build because n=0 day/metric combinations never enter agg_daily_metric. | as expected — agent-pinned live 2026-08-08 | ✓ |
| P2.3 | csv-export.md (`cli`) | After P2.1: head -1 /private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchpad/exports/utterances.csv && python3 -c "import csv; r=[x for x in csv.DictReader(open('/private/tmp/claude-501/-Users-kasonzha | Header is exactly 'utterance_id,child_id,display_name,day_local,scene,labels,symbol_count,word_count,tap_count,delete_count,ms_compose,ms_to_first_tap,used_suggestion,spoken,abandon_reason,function' — card labels only, NO utterance-text column (utterance text is a separate consent tier and never exp | as expected — agent-pinned live 2026-08-08 | ✓ |
| P3.1 | generated-artefacts.md (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && cp docs/analytics-metrics.md /private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchpad/am_before.md && python3 tools/gen_metric_index.py --db aac.db; ec | stdout 'regenerated metric index in /Users/kasonzhan/Documents/AI-Hackathon-AAC/docs/analytics-metrics.md', exit=0, then 'IDENTICAL' (regeneration is idempotent — the doc is machine-owned and the DB catalogue is unchanged). Markers found at lines 27 ('<!-- METRIC-INDEX:START -->') and 143 ('<!-- MET | as expected — agent-pinned live 2026-08-08 | ✓ |
| P3.2 | generated-artefacts.md (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && python3 -c "import json; [print(c, len(d['tools']), d['_about']['child_id'], d['_about']['window'], list(d['_about']['null_semantics'].keys())) for c in ['maya_t','sofia_r'] for d in [json.load(open(f'mcp/fixtu | Two lines: 'maya_t 14 maya_t last_14d [...]' and 'sofia_r 14 sofia_r last_14d [...]' — each checked-in fixture holds exactly 14 captured tool responses, _about.child_id matches the filename, and _about.null_semantics carries exactly the three keys ['value null + n > 0', 'value null + n = 0', 'direct | as expected — agent-pinned live 2026-08-08 | ✓ |
| P3.3 | generated-artefacts.md (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && python3 tools/gen_fixtures.py --db aac.db --child maya_t --window last_14d --out /private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchpad/fixture_live. | The fixture JSON IS written to the scratch path and contains 14 tools (final line prints '14'), proving the real MCP server (node mcp/server.ts over stdio) answers all 14 calls against the live DB — but the process prints a ValueError traceback ('… is not in the subpath of \'/Users/kasonzhan/Documen | as expected — agent-pinned live 2026-08-08 | ✓ |
| P4.1 | l2-rollup.md (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && S=/private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchpad && python3 -m tools.seed.generate --db $S/rollup_test.db >/dev/null && sqlite3 $S/rollup_tes | exit=0 and exactly these deterministic counts: 'agg_daily_metric       4,052' / 'agg_card_stats           190' / 'agg_cell_heat            127' / 'agg_word_pairs         2,045' / 'suppressed (n<min_n)   1,146', preceded by 'rollup complete in <t>s'. Identical counts to the shipped aac.db — the whole | as expected — agent-pinned live 2026-08-08 | ✓ |
| P4.2 | l2-rollup.md (`sql`) | sqlite3 /Users/kasonzhan/Documents/AI-Hackathon-AAC/aac.db "SELECT (SELECT COUNT(*) FROM agg_daily_metric a JOIN metrics_catalog m USING(metric_id) WHERE a.value IS NOT NULL AND a.n < m.min_n), (SELECT COUNT(*) FROM agg_daily_metric WHERE value IS NULL AND n = | 0\|0\|1146 — no value is ever published below its metric's min_n (write-time suppression), and every one of the 1,146 suppressed rows still carries n > 0, so a reader can tell 'measured but too thin' (value NULL, n>0) from 'never observed' (no row at all). | as expected — agent-pinned live 2026-08-08 | ✓ |
| P4.3 | l2-rollup.md (`sql`) | sqlite3 /Users/kasonzhan/Documents/AI-Hackathon-AAC/aac.db "SELECT metric_id, (SELECT COUNT(*) FROM agg_daily_metric a WHERE a.metric_id = m.metric_id) FROM metrics_catalog m WHERE metric_id IN ('suggestion_acceptance','vocabulary_gaps','visual_source_split',' | keyboard_use\|0, suggestion_acceptance\|0, vocabulary_gaps\|0, visual_source_split\|0 — the four NOT_YET_GENERATED metrics have zero agg_daily_metric rows (no source events emitted by any client). This is WHY the dashboard must render them as 'not recorded yet' and never as 0. | as expected — agent-pinned live 2026-08-08 | ✓ |
| P5.1 | rule-materialisation.md (`cli`) | After P4.1 (same scratch DB): cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && python3 tools/run_rules.py --db /private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchpad/rollup_test.db; echo "exit=$?" | exit=0. Header 'rules evaluated in <t>s  window 2026-07-25 .. 2026-08-07' (14-day window anchored to MAX(day_local)=2026-08-07 in the seed), then exactly: 'I1  1 child(ren)' / 'I2  1' / 'I3  1' / 'I4  5' / 'I5  1' / 'I6  1' / 'I7  1' / 'I8  1' / 'open findings: 12'. All eight rules fire — the seed-c | as expected — agent-pinned live 2026-08-08 | ✓ |
| P5.2 | rule-materialisation.md (`sql`) | sqlite3 /Users/kasonzhan/Documents/AI-Hackathon-AAC/aac.db "SELECT (SELECT COUNT(*) FROM fired_rules WHERE dismissed_at IS NULL AND superseded_by IS NULL), (SELECT MAX(json_array_length(evidence)) FROM fired_rules WHERE insight_id='I4'); SELECT insight_id \|\| ' | 12\|15 then the firing map: I1:maya_t / I2:jonah_k / I3:maya_t / I4:amara_o,jonah_k,liam_w,maya_t,sofia_r / I5:liam_w / I6:sofia_r / I7:sofia_r / I8:maya_t — 12 open findings, every planted persona fires its declared insights (EXPECTED_FIRING), and I4's evidence array is capped at exactly 15 rows per | as expected — agent-pinned live 2026-08-08 | ✓ |
| P5.3 | rule-materialisation.md (`sql`) | sqlite3 /Users/kasonzhan/Documents/AI-Hackathon-AAC/aac.db "SELECT (SELECT classification FROM fired_rules WHERE insight_id='I1' AND child_id='maya_t' AND superseded_by IS NULL), (SELECT COUNT(*) FROM fired_rules WHERE superseded_by='pending'), (SELECT COUNT(* | {"motor_share": 0.848, "semantic_share": 0.152, "verdict": "motor"}\|0\|0 — I1's classifier says Maya's mis-taps are 84.8% motor (the planted dead-row reach problem, not a conceptual one); no row is ever left stuck at superseded_by='pending'; every open finding carries thresholds_used so a therapist c | as expected — agent-pinned live 2026-08-08 | ✓ |
| P6.1 | seed-generation.md (`cli`) | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && S=/private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchpad && python3 -m tools.seed.generate --db $S/seed_a.db && python3 -m tools.seed.generate --db $ | Both runs print IDENTICAL counts: 'events            27,474' / 'utterances         6,340' / 'partner_turns      5,148' / 'board_revisions        1' / 'cards                 49' — generation is deterministic per-child (seeds 1001-1005), default 42 days ending 2026-08-07. Row IDs (ev_*/utt_*) DIFFER b | as expected — agent-pinned live 2026-08-08 | ✓ |
| P6.2 | seed-generation.md (`sql`) | sqlite3 /Users/kasonzhan/Documents/AI-Hackathon-AAC/aac.db "SELECT group_concat(child_id) FROM (SELECT child_id FROM children ORDER BY child_id); SELECT board_id, day_local, change_kind, card_id, from_row\|\|','\|\|from_col\|\|'->'\|\|to_row\|\|','\|\|to_col, actor_id, su | Line 1: 'amara_o,jonah_k,liam_w,maya_t,sofia_r' — exactly five children. Line 2: exactly one board_revisions row: 'board_maya_t\|2026-07-30\|move\|water\|3,0->0,2\|adult_patel\|I3' — the planted day-33 layout change (a well-meaning adult following I3 advice moved 'water'), which is what makes I8 fire for | as expected — agent-pinned live 2026-08-08 | ✓ |
| P6.3 | seed-generation.md (`sql`) | sqlite3 /Users/kasonzhan/Documents/AI-Hackathon-AAC/aac.db "SELECT (SELECT COUNT(*) FROM boards WHERE kind='mode'), (SELECT COUNT(*) FROM board_cells bc JOIN boards b ON b.board_id=bc.board_id WHERE b.kind='mode'), (SELECT COUNT(*) FROM mode_selection), (SELEC | 5\|0\|28\|highlight\|1.0 — five 'Snack time' mode boards exist (one per child, kind='mode'), they own ZERO board_cells rows (a mode holds no positions of its own — constraint C4), all 28 mode_selection rows are emphasis='highlight' (a mode highlights and dims, never moves a card), and position_consisten | as expected — agent-pinned live 2026-08-08 | ✓ |
| P7.1 | test-harnesses.md (`cli`) _[npm run concurrency (tools/concurrency_test.py)]_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && npm run concurrency; echo "exit=$?" | exit=0, runtime ~15s. Output shows 'database : <tmpdir>/concurrency.db  (copy — your data is untouched)', 'journal  : wal', a writer line and a readers line both with 'errors=0', reader p99 well under the 1000ms budget (typically single-digit ms), and a final line matching 'PASSED — <N> events writt | covered by npm run concurrency (tools/concurrency_test.py) — green this run | ✓ |
| P7.2 | test-harnesses.md (`curl`) _[test-api.sh J1]_ | curl -s -w '\n%{http_code}\n' -X POST http://localhost:3000/api/events -H 'content-type: application/json' -d '{"events":[{"event_id":"spec_j1","child_id":"maya_t","ts":1786100000000,"day_local":"2026-08-07","tz_offset_min":720,"session_id":"t","scene":"classr | HTTP 400 with body exactly: {"error":"events[0]: card_tap from 'board' requires grid_row — it cannot be reconstructed later"} — an on-grid card_tap missing grid coordinates is rejected loudly and nothing is written (grid_row/grid_col/grid_rows/grid_cols/nav_depth cannot be reconstructed after the fa | covered by test-api.sh J1 — green this run | ✓ |
| P7.3 | test-harnesses.md (`cli`) _[tools/test-api.sh (all 24 lettered cases)]_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && BASE=http://localhost:3000 ./tools/test-api.sh; echo "exit=$?" | exit=0 with '0 failed' in the final tally. Final line is either '24 passed  0 failed  0 blocked' (OpenAI credit available: E3 resolves via generated/hash_cache/semantic) or '23 passed  0 failed  3 blocked' (E3/E4/E5 report BLOCK on credit/quota/API-key errors). Sections run: E (visual ladder: E1/E2 | covered by tools/test-api.sh (all 24 lettered cases) — green this run | ✓ |
| P8.1 | verification-gate.md (`cli`) _[npm run verify (tools/verify.py, the 42-item gate: 41 checks + 1 named-gap warning)]_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && npm run verify > /private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchpad/verify_out.txt 2>&1; echo "exit=$?"; perl -pe 's/\e\[[0-9;]*m//g' /private/tm | exit=0; exactly 41 [PASS] lines across the four ordered sections (SAFETY 10, COVERAGE 17, RESTRAINT 1, INTEGRITY 13) and exactly 1 [WARN]: 'known gaps in seed data' naming suggestion_acceptance (needs suggestion_shown / suggestion_tap events), vocabulary_gaps (needs gap_detected events), visual_sour | covered by npm run verify (tools/verify.py, the 42-item gate: 41 checks + 1 named-gap warning) — green this run | ✓ |
| P8.2 | verification-gate.md (`sql`) _[verify.py SAFETY check 'board_change_proposals rejects move / resize / remove']_ | S=/private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchpad && cp /Users/kasonzhan/Documents/AI-Hackathon-AAC/aac.db $S/probe.db && sqlite3 $S/probe.db "INSERT INTO board_change_proposals (proposal_id, | The INSERT is REJECTED by the schema itself: 'Runtime error: stepping, CHECK constraint failed: change_kind IN ('add','add_copy','mask','unmask') (19)' and sqlite3 exits 19. 'move', 'resize' and 'remove' are impossible values for board_change_proposals.change_kind — relocating a learned button is bl | covered by verify.py SAFETY check 'board_change_proposals rejects move / resize / remove' — green this run | ✓ |
| P8.3 | verification-gate.md (`cli`) | After P4.1 or P6.1 (any scratch seed with views+rollup+rules, e.g. rollup_test.db after P5.1): cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && S=/private/tmp/claude-501/-Users-kasonzhan-Documents-AI-Hackathon-AAC/d802b9ce-9842-4fe8-a72a-e902dc381461/scratchp | exit=1 (non-zero — would fail the build). Grep shows exactly: '[FAIL] repeat_tap_rate polarity is neutral  polarity=lower_better — a neutral metric may not be styled as a warning' and 'FAILED — 1 check(s)' — proving the gate actually fails (not just prints) when the C1 neutrality invariant is violat | as expected — agent-pinned live 2026-08-08 | ✓ |

## auth — 4 features, 12 checks

| ID | Feature | Input | Expected | Actual | ✓ |
|---|---|---|---|---|---|
| AU1.1 | adult-sign-in.md (`curl`) | curl -s -D - -X POST http://localhost:3000/api/auth -H 'content-type: application/json' -d '{"username":"mpatel","password":"testing-12345"}' | HTTP 200, body exactly {"ok":true}. Response has header 'set-cookie: aac_adult=adult_patel.<13-digit ms expiry>.<base64url sig>; Path=/; Expires=<date ~12h ahead>; Max-Age=43200; HttpOnly; SameSite=lax'. Cookie value has exactly 3 dot-separated parts, first part is 'adult_patel'. NO 'Secure' attribu | as expected — agent-pinned live 2026-08-08 | ✓ |
| AU1.2 | adult-sign-in.md (`curl`) | Two requests: (a) curl -s -w '\nHTTP:%{http_code}\n' -X POST http://localhost:3000/api/auth -H 'content-type: application/json' -d '{"username":"mpatel","password":"wrong-pass"}' ; (b) same but -d '{"username":"no-such-user","password":"whatever-123"}' | Both (a) wrong password and (b) unknown username return HTTP 401 with the IDENTICAL body {"error":"Those details do not match"} — one message for both failure modes so response content never reveals which usernames exist (the unknown-username path even burns a decoy scrypt hash so timing matches). | as expected — agent-pinned live 2026-08-08 | ✓ |
| AU1.3 | adult-sign-in.md (`sql`) | sqlite3 aac_app.db "SELECT adult_id, username, length(password_hash), instr(password_hash,':') FROM adult_credentials ORDER BY adult_id;" then sqlite3 aac.db "SELECT COUNT(*) FROM sqlite_master WHERE name='adult_credentials';" | aac_app.db returns exactly 2 rows: 'adult_patel\|mpatel\|161\|33' and 'adult_slt\|sltdemo\|161\|33' — hash is scrypt salt:derived hex, 32 hex chars + ':' at position 33 + 128 hex chars = 161 total. aac.db count is 0: credentials deliberately live only in the app-owned DB, never in the pipeline-rebuilt aac | as expected — agent-pinned live 2026-08-08 | ✓ |
| AU2.1 | host-surface-routing.md (`curl`) _[test-api.sh G1/G7 cover the localhost cookie-less /dashboard 307 only; the Host-header matrix is not in any harness]_ | For each host H in {aac.kason.app, aac-dashboard.kason.app, aac-mcp.kason.app} and each path P in {/, /login, /dashboard, /api/reports, /api/mcp}: curl -s -o /dev/null -w '%{http_code}' -H "Host: $H" "http://localhost:3000$P" (no cookies) | Exact 15-cell matrix — aac.kason.app: /=307, /login=404, /dashboard=404, /api/reports=404, /api/mcp=404. aac-dashboard.kason.app: /=307 (redirect to /login, no session), /login=200, /dashboard=307, /api/reports=405 (route exists, POST-only), /api/mcp=404. aac-mcp.kason.app: /=404, /login=404, /dashb | covered by test-api.sh G1/G7 cover the localhost cookie-less /dashboard 307 only; the Host-header matrix is not in any harness — green this run | ✓ |
| AU2.2 | host-surface-routing.md (`curl`) | Three requests: (a) curl -s -D - -o /dev/null -H 'Host: aac-dashboard.kason.app' 'http://localhost:3000/dashboard/student/maya_t?tab=x' ; (b) curl -s -o /dev/null -w '%{redirect_url}' -H 'Host: aac-dashboard.kason.app' http://localhost:3000/ ; (c) curl -s -o / | (a) HTTP/1.1 307 with 'location: /login' — query string '?tab=x' is CLEARED, no ?next= return path. (b) redirect_url is http://localhost:3000/login (cookie-less dashboard-host root redirects to login, never serves the dashboard). (c) HTTP:500 — a forged aac_adult cookie passes the presence-only midd | as expected — agent-pinned live 2026-08-08 | ✓ |
| AU2.3 | host-surface-routing.md (`curl`) | curl -s -D - -o /dev/null -H 'Host: aac-dashboard.kason.app' http://localhost:3000/login \| grep -i 'x-robots-tag' | Exactly one header: 'x-robots-tag: noindex, nofollow, noarchive, nosnippet' on the 200 response. Same header must be present on any /dashboard* path response regardless of host (localhost included), so children's names can never be indexed or archived. | as expected — agent-pinned live 2026-08-08 | ✓ |
| AU3.1 | login-page.md (`curl`) | curl -s http://localhost:3000/login (save body), plus curl -s http://localhost:3000/api/auth | /login is HTTP 200 and the HTML contains 'Sign in' (h1 and submit button) and the subtitle 'The dashboard shows children’s communication records.' Because an account already exists, GET /api/auth returns exactly {"setupOpen":false}, and the /login HTML contains ZERO occurrences of: 'Set up the dashb | as expected — agent-pinned live 2026-08-08 | ✓ |
| AU3.2 | login-page.md (`curl`) | Three requests to POST http://localhost:3000/api/auth with -H 'content-type: application/json': (a) -d '{"username":"x","password":"y","adultId":"adult_slt","setup":true}' ; (b) -d '{"username":"mpatel"}' ; (c) -d 'not-json' | (a) HTTP 409 {"error":"Setup is closed — an account already exists"} — the first-run branch is re-checked server-side and is dead forever once one account exists. (b) HTTP 400 {"error":"Username and password are required"}. (c) HTTP 400 {"error":"Body must be JSON"}. | as expected — agent-pinned live 2026-08-08 | ✓ |
| AU3.3 | login-page.md (`browser`) | Browser at http://localhost:3000/login: (1) type username 'mpatel', password 'bad-password', click 'Sign in'; (2) then correct to password 'testing-12345', click 'Sign in' | (1) stays on /login, a red error box appears containing exactly 'Those details do not match'; the submit button reads 'Working…' while the request is in flight and is disabled. (2) navigates to /dashboard which renders header identity 'Mrs Patel' with role 'teacher', the class view heading area show | as expected — agent-pinned live 2026-08-08 | ✓ |
| AU4.1 | role-consent-scoping.md (`sql`) | sqlite3 aac.db "SELECT c.child_id, c.display_name, r.relation FROM children c JOIN roster r ON r.child_id = c.child_id WHERE r.adult_id = 'adult_patel' AND (r.expires_at IS NULL OR r.expires_at > CAST(strftime('%s','now') AS INTEGER) * 1000) ORDER BY c.display | adult_patel: exactly 5 rows, all relation 'teacher', in this order: amara_o\|Amara O.\|teacher, jonah_k\|Jonah K.\|teacher, liam_w\|Liam W.\|teacher, maya_t\|Maya T.\|teacher, sofia_r\|Sofia R.\|teacher. adult_parent_maya: exactly 1 row: maya_t\|Maya T.\|parent. This is the literal visibleChildren() query from | as expected — agent-pinned live 2026-08-08 | ✓ |
| AU4.2 | role-consent-scoping.md (`curl`) | Sign in as mpatel (cookie jar from AU1.1), then: curl -s -b <jar> http://localhost:3000/dashboard/student/maya_t/access | HTTP 200. The HTML contains the consent-lock copy exactly: 'Available to speech therapists. You have teacher access.' (partner_speech tier: TIER_RELATIONS allows only slt, and no child has partner_speech consent anyway). The locked panel is SHOWN, not hidden: the page contains both 'You can see' and | as expected — agent-pinned live 2026-08-08 | ✓ |
| AU4.3 | role-consent-scoping.md (`curl`) | With the valid mpatel cookie jar: curl -s -o /dev/null -w 'HTTP:%{http_code}' -b <jar> http://localhost:3000/dashboard/student/nobody_x ; then sqlite3 aac.db "SELECT COUNT(*) FROM roster WHERE adult_id='adult_patel' AND child_id='nobody_x';" | HTTP:500 — requireChild(viewer,'nobody_x') throws 'NOT_AUTHORISED: adult_patel has no active roster row for nobody_x' during server render; the page never returns 200 for an off-roster child. The SQL returns 0, confirming why. Same throw maps to HTTP 403 on /api/chat and /api/reports (msg.startsWith | as expected — agent-pinned live 2026-08-08 | ✓ |

## deploy — 5 features, 15 checks

| ID | Feature | Input | Expected | Actual | ✓ |
|---|---|---|---|---|---|
| DP1.1 | cloudflare-tunnel-ingress.md (`curl`) | The tunnel's three hostnames share one origin; middleware.ts splits them by Host header. Run against the local origin with spoofed Host headers: (a) curl -s -o /dev/null -w '%{http_code}' -H 'Host: aac.kason.app' http://localhost:3000/dashboard ; (b) curl -s - | (a) 404. (b) body is exactly the 9 characters 'Not found' (bare 404, not a redirect). (c) 404 (mcp surface allows only paths starting /api/mcp). (d) 404 (event ingest is kid-surface-only; dashboard host may not POST /api/events). (e) 404 (shared assets are excluded from the mcp surface). (f) 200 (sa | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP1.2 | cloudflare-tunnel-ingress.md (`curl`) _[test-api.sh L1 and L2 (same checks without the Host spoof; localhost maps to surface 'any' so the token guard is exercised either way)]_ | The aac-mcp hostname has no Cloudflare Access policy; its only guard is the bearer token checked inside the route. Run: curl -s -w '\n%{http_code}' -X POST -H 'Host: aac-mcp.kason.app' -H 'content-type: application/json' -d '{"jsonrpc":"2.0","id":1,"method":"p | Both requests return HTTP 401 with body exactly {"jsonrpc":"2.0","id":null,"error":{"code":-32001,"message":"Unauthorised"}} — the route fails closed when the token is absent or wrong. | covered by test-api.sh L1 and L2 (same checks without the Host spoof; localhost maps to surface 'any' so the token guard is exercised either way) — green this run | ✓ |
| DP1.3 | cloudflare-tunnel-ingress.md (`cli`) | Pin the tunnel config and its launchd job: (a) cat /Users/kasonzhan/Documents/AI-Hackathon-AAC/deploy/aac.yml ; (b) plutil -p /Users/kasonzhan/Documents/AI-Hackathon-AAC/deploy/app.kason.aac.tunnel.plist ; (c) launchctl list \| grep app.kason.aac.tunnel | (a) tunnel: aac; credentials-file: /Users/kasonzhan/.cloudflared/f50f1ef0-ea82-4ec2-953b-93404512a766.json; exactly 4 ingress rules IN ORDER: hostname aac.kason.app -> service http://localhost:3000, hostname aac-dashboard.kason.app -> http://localhost:3000, hostname aac-mcp.kason.app -> http://local | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP2.1 | next-build-config.md (`cli`) | Pin the entire Next.js config: (a) grep -c "serverExternalPackages: \['node:sqlite'\]" next.config.ts ; (b) grep -c "bodySizeLimit: '1mb'" next.config.ts ; (c) grep -cE "headers\|redirects\|rewrites\|basePath\|output" next.config.ts (run all from /Users/kasonzhan/ | (a) 1 — node:sqlite is the single serverExternalPackages entry. (b) 1 — Server Action bodies capped at 1mb under experimental.serverActions. (c) 0 — the config deliberately contains no headers/redirects/rewrites/basePath/output keys; host routing and the X-Robots-Tag header live in middleware.ts ins | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP2.2 | next-build-config.md (`cli`) | Prove the server-only database boundary holds in the built bundle: (a) grep -rl 'node:sqlite' .next/static \| wc -l ; (b) grep -rl 'node:sqlite' .next/server \| wc -l (from repo root, after a production build exists) | (a) 0 — no client-delivered chunk under .next/static references node:sqlite, so no database driver ships to the child's tablet. (b) >= 1 (24 files on the current build) — the reference exists only in server-side output. | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP2.3 | next-build-config.md (`curl`) | Headers are middleware's job, not the config's: curl -s -D - -o /dev/null -H 'Host: aac-dashboard.kason.app' http://localhost:3000/login \| grep -i 'x-robots-tag' ; then curl -s -D - -o /dev/null -H 'Host: aac.kason.app' http://localhost:3000/who \| grep -ci 'x- | First command prints exactly one header line: x-robots-tag: noindex, nofollow, noarchive, nosnippet (set by middleware.ts on every dashboard-surface response, even /login). Second command prints 0 — the kid surface carries no X-Robots-Tag. | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP3.1 | npm-scripts.md (`cli`) | Pin all 8 npm scripts and the engine floor: python3 -c "import json; p=json.load(open('/Users/kasonzhan/Documents/AI-Hackathon-AAC/package.json')); print(json.dumps(p['scripts'],indent=1)); print(p['engines'])" | Exactly 8 scripts: build='./tools/build.sh aac.db' (rebuilds the DATABASE, destroying aac.db), mcp='node mcp/server.ts --db aac.db', verify='python3 tools/verify.py --db aac.db', concurrency='python3 tools/concurrency_test.py --db aac.db', dev='next dev', build:web='next build' (builds the WEB APP), | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP3.2 | npm-scripts.md (`cli`) | Pin the dependency statement (zero SQLite packages, zero MCP/AI SDKs): (a) python3 -c "import json; p=json.load(open('/Users/kasonzhan/Documents/AI-Hackathon-AAC/package.json')); print(sorted(p['dependencies'])); print(sorted(p['devDependencies']))" ; (b) ls / | (a) dependencies exactly ['next','react','react-dom']; devDependencies exactly ['@tailwindcss/postcss','@types/node','@types/react','@types/react-dom','tailwindcss','typescript']. (b) 0 — no sqlite npm package installed; all DB access uses the node:sqlite builtin. (c) a version >= v24 satisfying eng | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP3.3 | npm-scripts.md (`cli`) | Prove Node runs the .ts MCP server directly with no compile step (what engines >=24 buys), and that .mcp.json mirrors the npm script: (a) cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && echo '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' \| node mcp/server. | (a) prints exactly 20 — the stdio server answers tools/list with 20 tools on the first stdout line and exits cleanly on stdin EOF. (b) prints: node ['mcp/server.ts', '--db', 'aac.db'] — identical invocation to the 'mcp' npm script. | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP4.1 | production-web-service.md (`cli`) | The launchd job is up, launchd's PID IS Node's PID (the exec contract), and the origin answers: (a) launchctl list \| grep app.kason.aac.web ; (b) lsof -nP -iTCP:3000 -sTCP:LISTEN ; (c) curl -s -o /dev/null -w '%{http_code}' http://localhost:3000/who | (a) one line '<pid>\t0\tapp.kason.aac.web' (numeric PID, last exit status 0). (b) exactly one LISTEN entry: COMMAND 'node' on TCP *:3000, whose PID equals the PID from (a) — proving run-web.sh's final 'exec' replaced the shell, so launchd supervises Node directly. (c) 200. | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP4.2 | production-web-service.md (`cli`) | Build guard and start line: (a) test -s /Users/kasonzhan/Documents/AI-Hackathon-AAC/.next/BUILD_ID && cat .next/BUILD_ID ; (b) grep '\[run-web\] starting next' deploy/logs/web.log \| tail -1 ; (c) grep -F '[run-web] No production build. Run: npx next build' dep | (a) succeeds; BUILD_ID is a non-empty opaque id (currently 'OiNT8W8MmOSH3E9pi5XSv', changes every 'npx next build'). (b) the line is exactly '[run-web] starting next on :3000 (build <BUILD_ID>)' where <BUILD_ID> matches the current contents of .next/BUILD_ID. (c) 1 — the refusal message says 'npx ne | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP4.3 | production-web-service.md (`cli`) | Environment injection and precedence: (a) plutil -p deploy/app.kason.aac.web.plist ; (b) /usr/libexec/PlistBuddy -c 'Print :EnvironmentVariables:AAC_MCP_TOKEN' deploy/app.kason.aac.web.plist \| tr -d '\n' \| wc -c ; (c) grep -n 'set -a; . ./.env.local; set +a' d | (a) Label = app.kason.aac.web; ProgramArguments = exactly ['/Users/kasonzhan/Documents/AI-Hackathon-AAC/deploy/run-web.sh'] (one element — not npm, not node); WorkingDirectory = /Users/kasonzhan/Documents/AI-Hackathon-AAC; RunAtLoad true; KeepAlive true; ThrottleInterval 10; EnvironmentVariables con | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP5.1 | typescript-and-css-toolchain.md (`cli`) _[npm run typecheck]_ | cd /Users/kasonzhan/Documents/AI-Hackathon-AAC && npx tsc --noEmit; echo "exit=$?"  (identical to 'npm run typecheck') | Prints 'exit=0' with no compiler diagnostics — the whole repo (app/, lib/, components/, mcp/, tools/-adjacent .ts, and the generated .next/types/**) typechecks clean under strict mode. | covered by npm run typecheck — green this run | ✓ |
| DP5.2 | typescript-and-css-toolchain.md (`cli`) | Pin the load-bearing tsconfig options: python3 -c "import json; t=json.load(open('/Users/kasonzhan/Documents/AI-Hackathon-AAC/tsconfig.json')); c=t['compilerOptions']; print(c['strict'], c['noEmit'], c['allowImportingTsExtensions'], c['module'], c['jsx'], c['p | Prints: True True True esnext preserve {'@/*': ['./*']} then ['next-env.d.ts', '**/*.ts', '**/*.tsx', '.next/types/**/*.ts'] then ['node_modules']. In particular: allowImportingTsExtensions=true (required for 'node mcp/server.ts' to run TypeScript directly), '@/*' maps to the repo ROOT (not src/), a | as expected — agent-pinned live 2026-08-08 | ✓ |
| DP5.3 | typescript-and-css-toolchain.md (`cli`) | Tailwind v4 is configured from CSS, with no tailwind.config file: (a) find /Users/kasonzhan/Documents/AI-Hackathon-AAC -maxdepth 1 -name 'tailwind.config.*' \| wc -l ; (b) cat postcss.config.mjs ; (c) head -1 app/globals.css ; (d) grep -c -- '--color-accent: #2 | (a) 0 — no tailwind.config.* exists at the repo root. (b) the plugins object contains exactly one entry: '@tailwindcss/postcss': {} (no options, no other plugins). (c) exactly: @import 'tailwindcss'; (d) 1 — the light-theme accent token is defined once in the @theme block (a dark-mode override uses | as expected — agent-pinned live 2026-08-08 | ✓ |

## Failures & fixes this run

| # | What failed | Root cause | Fix | Re-verified |
|---|---|---|---|---|
| 1 | Every `?child=` link 500'd the production board | `cookies().set()` inside a Server Component render — legal in dev, throws in prod | New `GET /api/session/switch` Route Handler owns the write; page redirects to it | H1–H4 in test-api.sh + Playwright A-suite on the prod build |
| 2 | Insight cards unreachable in every build since the initial commit | `InsightList` was never imported by any page — two clinical invariants (informational ⇒ no action button; forbidden_actions displayed) lived in unmounted code | `toCardData` adapter in `lib/insights.ts`; Findings section mounted on the student page | Browser: 4 cards for maya_t; I4 (informational) shows **no** action list, I1/I3/I8 do; forbidden actions visible on all; 375 px clean |
| 3 | Chatbot would answer from a deleted database after `tools/build.sh` | Chat bridge cached its `Db` handle in a global with no inode check | `db()` re-stats per call, reopens on inode change; metric-id enum re-keyed per handle (`WeakMap`) | tsc + SSE probe on rebuilt server |
| 4 | Chat `TODAY` anchor off by one for half of every NZ day | `toISOString()` is UTC | `toLocaleDateString('en-CA')` (server-local) | Code + SSE probe |
| 5 | Working dashboard password sat in a committable skill file | QA recipe hardcoded credentials | Moved to gitignored `.env.local` (`AAC_QA_USER/PASS`); skill sources them | grep — zero hits outside `.env.local` |
| 6 | H3 test failed on first run | My check, not the app: HTTP headers end CRLF, `grep '/$'` missed `\r` | `tr -d '\r'` before matching | Suite 28/28 |
| 7 | Modeling-help dialog ignored Escape | Overlay claimed `aria-modal` but had no keyboard handling — only backdrop/button click closed it | `keydown` listener + initial focus on the close button (`components/kid/modeling-help.tsx`) | Browser: dialog opens, Escape closes |
| 8 | `J2` flaked once per interrupted run | Fixed `event_id` + cleanup `DELETE` that silently loses to `SQLITE_BUSY` under WAL — a stale row makes the next run's insert a dedupe | Per-run event ids in the J-section; `.timeout 3000` on cleanup | 28/28 twice back-to-back, 0 leftovers |
| 9 | `hungry`/`play` icons failed every quota round | Bare-concept prompts repeatedly produced no image from `gemini-2.5-flash-image` | Reworded as concrete pictogram scenes (face + empty bowl; ball + block) | 76/76 on disk, manifest regenerated, 30 icons live on the board |

_Total checks: 194 · ✓ 191 · ◻ 3 (each with reason)_


## Metric-calculation pass — 2026-08-08 (half-year cohort) · build `0s5ykVNfwek-JlydxRZ1h`

Every metric in `docs/AAC_Filtered_Metric_Index.md` was recomputed independently from
raw events on a **180-day, 5-child seeded cohort** (113,914 events, 25,641 utterances —
`tools.seed.generate --days 180`) and compared across three layers: the daily views/
rollup, the dashboard reader (`lib/metrics.ts`/`lib/report.ts`), and MCP `get_metrics`.
Final state: **135/135 scalar comparisons and 15/15 dimensional comparisons agree with
independent gold recomputation** (9 scalar metrics × 5 children × 7/14/28-day windows;
cell_heat, card_frequency, nav_depth_by_card, word_pairs sampled per child). Live-data
three-way check: gold 0.8466 correction adjacency / 88 modelled presses = dashboard
"85% next door" = chatbot "88 times… 0.8466". Harness:
`scratchpad/metric_check.py` (session artifact, reproducible).

Calculation bugs found and fixed by this pass:

| # | Bug | Measured impact | Fix |
|---|---|---|---|
| 10 | Daily min_n suppression NULLed values at write time, destroying window aggregates | correction_adjacent_rate 28d read 0.15 from 53 samples when 170 samples said 0.51; partner_wait_time vanished entirely (45–178 real samples → "no data") | `rollup.py` stores true values; every reader now suppresses at its own grain (window readers on window n, day readers per day) |
| 11 | MCP get_metrics used one unweighted AVG for every metric | teacher_modeling: 348 presses reported as 12.4; silence_streak meaningless mid-window average | per-metric rollup (sum / last / weighted mean) mirroring `lib/metrics.ts`, + window-grain suppression |
| 12 | Dashboard `report.ts windowValue()` had the same uniform AVG | same class as #11, on the student page | delegates to `readMetric` — one aggregation implementation for the whole app |
| 13 | `run_rules.py` could never re-run: `superseded_by='pending'` sentinel violates its own FK on any non-empty table | the "materialised nightly" story crashed on night one; only ever worked on fresh builds | supersede chain held in Python, no sentinel; re-run proven (24 rows, 12 superseded, 0 dismissals lost) |
| 14 | repeat-run detection let a delete of the same card extend runs backwards | "A, del, A, A, A" hid a legal run — undercounts vs the catalogue's "with NO delete" formula | tap-type-aware lag chain in `v_m_repeat_tap_rate` |
| 15 | correction adjacency compared against the next EVENT (possibly another delete), not the next TAP | wrong replacement coordinates when deletes chain | `v_mistaps` joins the next card_tap |
| 16 | `new_words` daily value was a proportion while catalogue unit is count; readers summed proportions | window number was neither a count nor a clean proportion | `v_daily_metrics_all` feeds `v_new_words_count`; proportion view kept for rule I6 |
| 17 | Zero new words in a window reported as "not collected" | the catalogue's own example (0 after card additions = the additions missed) was unreportable | tally metrics zero-fill when the child was present; MCP gains honest `no_data_in_window` reason |
| 18 | verify.py plant assertions assumed the 42-day horizon | at 180 days the gate failed I2 although the windowed rule was CORRECT (planter silent 142 days) | data-driven softening: stale plants warn instead of fail |

Gate state after: `verify.py` **PASSED** on both the 180-day cohort (3 informative
warnings) and the live database · `test-api.sh` 28/28 · repeat-run of `run_rules.py`
preserves all dismissals.

---

## Run 3 — 2026-08-08 morning · full retest on PUBLIC hostnames (BUILD_ID at close: see git 73c177a..HEAD)

Scope change from earlier runs: every browser and curl row executed against
**https://aac.kason.app / aac-dashboard.kason.app / aac-mcp.kason.app** (or
localhost with a spoofed `Host:` header), after the icon 404 proved that
localhost-only testing hides hostname-scoped failures.

### Suites
| Suite | Result |
|---|---|
| `tools/test-api.sh` | **33/33** (new: K1–K4 hostname static assets · L5 invalid-window rejection) |
| `npm run verify` | **PASSED**, 1 known warning (documented not-collected gaps) |
| `npm run concurrency` | 116,480 writes + 108,489 reads, 0 lock errors |
| `npm run typecheck` | clean |

### Browser rows (public URLs, evidence recorded in session)
- /who: 5 children; console clean. Board icons **36/36 loaded** (post-fix).
- Sentence assembly: “I want water.” · “I want more.” · Undo → “I want.” exact.
- Mode safety (while modes existed): 36-card order byte-identical entering
  Snack time, 30 dimmed. Modes were then **removed from the live db by request**
  (boards/mode_selection + 6 same-day test events; FK check clean; verify PASSED).
  A pipeline rebuild would re-seed them.
- Modelling dialog: opens, Escape closes. ABC keyboard: QWERTY, Escape now closes
  (bug #21). 375 px: no horizontal scroll.
- Icon geometry after resize work: 375×812 → 42×42 · 820×1180 → 106×106 ·
  1440×900 → 99×99, **0 overflowing card children at all three widths**.
- Dashboard: login ✓; queue ranks Jonah first (“Has not spoken for 5 days”);
  Findings on student page — I4 renders no action list, forbidden actions
  visible, **Yes button now answers visibly** (bug #19); every metric carries
  `n = …`; partner-wait renders “not recorded”, never 0; student + class pages
  clean at 375 px; metric rows pack with **0 trailing empty px** (flex grow).
- Chat SSE (public): tool_call → tool_result → notices → text → done; grounded
  “Jonah has not communicated in the last 5 days”; move-the-buttons question
  answered with mask/add-copy alternatives, never relocation.
- MCP (public): 20 tools; `last_28d` teacher_modeling amara_o = **372 n=372**,
  equal to direct SQL; UPDATE rejected (`SQL_REJECTED`); `move` refused
  (read-only transport + schema enum); invalid window rejected loudly (bug #22).

### Bugs found and fixed this run
| # | Bug | Fix |
|---|---|---|
| 19 | Insight card “Yes — show me the detail” was a no-op when the numbers were already open (user-reported) | confirmed state + scroll-into-view + footer feedback (`components/insight-card.tsx`) |
| 20 | Long labels (“different”) widened the card’s inner flex column past the button: icon overflowed the card, labels collided (user-reported, iPhone) | `min-w-0 max-w-full` on the face column, `overflow-wrap:anywhere` on labels (`components/kid/card-face.tsx`) |
| 21 | ABC keyboard layer claims `aria-modal` but ignored Escape | document keydown → onClose (`components/kid/keyboard-layer.tsx`) |
| 22 | All 7 window-taking MCP tools silently fell back to 7 days on an unknown window token (`"28d"` → week labelled as month) | `resolveWindow` throws `SQL_REJECTED` naming allowed tokens (`mcp/tools.ts`); pinned by L5 |
| 23 | Board icons fixed at 36 px regardless of screen; squeezed to 32×73 (distorted) at phone width after first resize attempt | responsive `clamp(40px, 11vh, 112px)` + width cap with `aspect-ratio: 1` (`card-face.tsx`, `board-app.tsx`) |

### Product changes by user request (same session)
- “Snack time” mode chips removed from the live install (data-level; feature intact in code).
- Findings section moved below the metric sections on the student page.
- Metric card rows: wrap-and-grow flex + 1400 px max content width — no empty tracts.
- Icons enlarged at every breakpoint; category drawer + essential rail bumped to match.
- `/icons/ai/*` now served with `Cache-Control: max-age=86400, stale-while-revalidate=604800`
  (were `max-age=0`: 76 revalidations per board load).
